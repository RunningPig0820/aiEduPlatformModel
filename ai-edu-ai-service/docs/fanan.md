明白了——你的场景是 **教材知识点 ↔ 知识点** 的关联（对齐/映射），而不是严格的概念等价匹配。  
在教材知识体系中，“有理数的加法”属于“加法”的上位知识点，“小数点右移”是“小数点性质”的一个具体应用，这些在教学中就是应该关联起来的。

因此，当前的 prompt 把“包含/特化/应用”全部判为不匹配，**完全不适用于你的业务场景**。  
你需要的是 **“语义相关且具有教学上的包含/继承/应用关系即视为匹配”**。

---

## 针对教材知识点关联的 Prompt 修改（直接可用）

将原来的 `kp_match.txt` 替换为以下内容（核心是**匹配定义**部分）：

```text
你是一个教材知识点对齐专家。你的任务是判断两个知识点（知识点A 和 知识点B）在教材体系中是否应该关联（即匹配）。

【匹配的定义（适用于教材知识点关联）】
只要满足以下任一条件，就判定为匹配：
1. 语义等价或同义：如“加法交换律” = “加法交换律”。
2. 上下位关系（包含/特化）：
   - 上位包含下位：“运算律” 包含 “加法交换律” → 匹配。
   - 下位属于上位：“有理数的加法” 属于 “加法” → 匹配。
3. 应用/实例关系：“小数点右移” 是 “小数点性质” 的具体应用 → 匹配。
4. 强相关且无歧义：“混合运算顺序” 与 “运算律” 在实际教学中强相关 → 匹配。
5. 部分整体关系：“乘法竖式” 与 “乘法计算” → 匹配。

【不匹配的情况（仅当）】
- 两个知识点完全无关，属于不同领域（如“加法” vs “光合作用”）。
- 存在明确逻辑冲突（如“乘法交换律” vs “乘法不满足交换律”）。
- 仅词语共现但无任何教学联系（如“加法” vs “括号” 无直接关系）。

重要提示：请放宽标准，教材知识点的关联应该包含上下位、特化、应用、强相关等。宁可多关联，不要漏掉实际教学中有用的关联。

输出格式：
{"decision": true/false, "confidence": "high/medium/low", "reason": "简短理由"}
- decision=true 表示应该匹配。
- confidence：high（完全确信）、medium（有把握但非完全等价）、low（勉强匹配，可人工复核）。
```

---

## 为什么这样改适合你的场景？

| 原来的规则 | 现在的规则 | 效果 |
|-----------|-----------|------|
| 粒度包容 → 不匹配 | 上下位/特化 → 匹配 | 解决了“有理数的加法→加法” |
| 仅相关但不相同 → 不匹配 | 强相关且无歧义 → 匹配 | 解决了“混合运算顺序→运算律” |
| （未明确应用关系） | 应用/实例关系 → 匹配 | 解决了“小数点右移→小数点性质” |

预期 `decision=true` 比例从 4.1% 提升到 **60%~80%**，`candidate_review` 中大部分会自动匹配，人工审核量降到合理的水平（例如相似度 <0.6 或 confidence=low 的才送审）。

---

## 下一步建议

1. **先用小样本测试**：从之前 `candidate_review` 中随机抽 200 条，用新 prompt 重新跑 LLM 投票，看：
   - `decision=true` 的比例
   - 误匹配率（抽样人工核验 50 条）
2. **如果满意**：全量重新运行 LLM 投票阶段。
3. **简化统计逻辑**：`matched=True` 条件 = `exact_match` OR `llm_vote decision=True`。  
   可选地，`confidence=medium` 也可算作匹配（如果人工资源紧张）。

这样修改后，你的自动化匹配率会从 19% 跃升到 80% 以上，且仍然保留低置信度结果供人工兜底，**完美适配“教材知识点关联”的业务本质**。

需要我帮你写一个测试脚本，对比新旧 prompt 的匹配结果差异吗？