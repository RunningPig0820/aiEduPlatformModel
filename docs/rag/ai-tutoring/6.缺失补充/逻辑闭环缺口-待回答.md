# 逻辑闭环缺口（待回答）

> 目的：完善文档 8 节（`4.完善文档/`）只写了"扬长"，缺"防御性追问"承接。以下 8 个逻辑闭环缺口，来自问题表 `ai-tutoring.md` 的"可能/防御要点" vs 代码实际的对照。
> 用法：每个缺口下 **`📝 你的回答`** 留空，由你填写最终口径；填好后回填进完善文档对应节，补成"介绍 + 能接追问"。
> 真值源：`3.代码/` 分析 + `git/坑档案.md` + `OpenSpec-代码对账.md`（差异都在这里，回答不能与代码矛盾）。

---

### 缺口 1：怎么防学生套答案？（问题表提"行为风控 + 题目指纹"，代码没有）

- **问题表依据**：操作节「怎么防学生套答案？」→ 亮点"行为风控（高频/无思考）+ 题目指纹去重"；「触发了怎么办？」→ "要求先作答再继续"
- **期望逻辑**：识别"套答案行为"（高频、无思考、重复题）→ 拦截/要求先作答 → 题目指纹防重复刷题
- **代码实际**：只有**确定性护栏**（答案 2 次 / 轮次 20 / 安全终止 / 结构化降级），**无行为风控模型、无题目指纹去重**（规划阶段 3）
- **闭环缺口**：面试官按问题表问"怎么防套答案"时，正文只能答护栏，答不出"行为风控/指纹为什么没有、实际靠什么替代"
- **📝 你的回答（我来填）**：

---

### 缺口 2：没有题库，答疑质量靠什么保证？（问题表提"题库匹配 → 没命中 → 待入库"）

- **问题表依据**：操作节「一次完整答疑怎么走？」→ 亮点"意图分类 → 题型识别 → **题库匹配** → 分步引导 → 答案校验 → 图谱点亮"；「没命中题库呢？」→ "通用引导 + 标记待入库"
- **期望逻辑**：答疑要能"匹配到标准解法"，题库是质量底座
- **代码实际**：**无题库（MVP 设计）**——答疑靠 LLM 生成引导，题型识别（question-understand）是另一条独立线（题型分析页），不参与答疑匹配
- **闭环缺口**：正文没写"没有题库，答疑靠 LLM 引导，怎么保证不胡说"；面试官按问题表问"题库匹配"会答不出
- **📝 你的回答（我来填）**：

---

### 缺口 3：意图分类现在到底是什么？（问题表提"三级意图分类"，代码演进为"两分法"）

- **问题表依据**：介绍节「和普通聊天机器人有什么区别？」→ 防御要点"三级意图分类 + 引导式教学 + 防作弊 + 图谱点亮"
- **期望逻辑**：答疑先分"学术/引导/无关"三级意图，再决定怎么回应
- **代码实际**：演进为**动作闭集两分法**——decide 只判"是否在答题"（在答题→hint/approach 引导；不在→concept 拉回），由 `type` 闭集承载；无显式"三级意图"枚举
- **闭环缺口**：正文把"三级意图"一句带过（演进故事），没讲清楚现在的判定模型是什么样
- **📝 你的回答（我来填）**：

---

### 缺口 4：为什么掌握度按题型、不按知识点？（最大翻转，缺"为什么"论证）

- **问题表依据**：数据关联节「答疑和知识图谱怎么联动？」→ "AI预判 + 学生确认 → 条件点亮 → **更新掌握度**"
- **期望逻辑**：掌握度 = 知识点掌握程度（图谱点亮维度）
- **代码实际**：掌握度主体 = **题型**（canonical → ScoreMapper 累计平均）；知识点关联由 `kp-matching-lightup` 独立承担（题型→知识点 ADMIN 维护）
- **闭环缺口**：05 讲了"怎么算"，没讲"**为什么翻转**"——题型相比知识点的好处（题型可观测、可打分、题目自然聚到题型；知识点依赖图谱映射且易错）
- **📝 你的回答（我来填）**：

---

### 缺口 5：为什么决策权（护栏）放 Java 而不是 Python？（深度论证）

- **问题表依据**：难点节常问"为什么这么设计"
- **期望逻辑**：审批放 Python（脚本/图节点）看似省调用
- **代码实际**：护栏全在 Java（`TutoringGuardrailService`），Python 只输出软意图
- **闭环缺口**：03 有"球员不能当裁判/防提示词攻击"两句，缺完整论证：① 数据在 Java（审批后果要回 Java 执行）② LLM 层可被骗（防提示词攻击）③ 规则数字可配置运营 ④ Python 无状态可水平扩展
- **📝 你的回答（我来填）**：

---

### 缺口 6：为什么刻意不用 agent 编排？（深度论证）

- **问题表依据**：难点节常问"为什么不用 LangChain agent"
- **期望逻辑**：答疑像智能体（对话天然 agent 形态）
- **代码实际**：MVP = L0 两端点 + Java 护栏，无 agent 循环；决策靠 Java 计数器不靠 LLM
- **闭环缺口**：08 有"苏格拉底硬约束/规则确定/成本可测"一句话，缺完整论证：① 苏格拉底引导是教育硬约束，不能交给 LLM 自主 ② 业务规则（20轮/2次答案/掌握度）必须确定 ③ 成本与可测性（调用边界清晰、单步可测）④ 阶段 2 升级 LangGraph 时护栏仍在 Java（确定性边界保住）
- **📝 你的回答（我来填）**：

---

### 缺口 7：知识点会不会选错？（问题表防御要点，双保险具体怎么工作）

- **问题表依据**：数据关联节「知识点会不会选错？」→ 防御要点"AI 预判 + 学生确认双保险，不空点不误点"
- **期望逻辑**：AI 猜知识点 → 学生确认 → 才点亮
- **代码实际**：低置信澄清卡——decide 输出 label → `resolveKp`（PENDING+candidates）→ 学生选/跳过（`voteKp`）→ 点亮；跳过弃权；**答疑链路不直接写 Neo4j**（点亮归 kp-matching-lightup）
- **闭环缺口**：05 有联动，但"双保险"的完整流程（什么时候触发澄清卡、学生不选怎么办、PENDING 是什么）没展开
- **📝 你的回答（我来填）**：

---

### 缺口 8：答疑/掌握度数据与图谱、学情的完整闭环（"为什么点亮 → 学习进度可视化"）

- **问题表依据**：数据关联节「为什么要点亮？」→ "学习进度可视化"
- **期望逻辑**：答疑产生的数据 → 图谱/学情 → 反馈给学生/家长，形成"答疑-分析-迭代"闭环
- **代码实际**：掌握度→题型掌握页（分页/分桶/查看原题跳回）；错误事件→学情（未来）；**家长端报表/多维画像未落地（阶段 3）**
- **闭环缺口**：05 有数据流图，但"消费方到底谁在看、家长端有没有"的边界没讲清（家长端未落地，只能答"数据面已提供"）
- **📝 你的回答（我来填）**：

---

## 回填规则（你填完后）

- 缺口 1/2 → 回填 `04-安全与防作弊` 或 `02-一次完整答疑怎么走`
- 缺口 3 → 回填 `01-模块定位`
- 缺口 4/7/8 → 回填 `05-数据落库与掌握度`
- 缺口 5/6 → 回填 `03-架构` / `08-演进路线`
- 回填时加「追问与防御」小节，格式：`**预期追问：** ... → **回答要点：** ...`
