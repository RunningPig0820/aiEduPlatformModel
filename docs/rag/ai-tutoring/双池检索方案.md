# RAG 双池检索方案（全量池 + 切片池）

> 状态：**已落地**（2026-08-26，双池入桶 + 多模块 intent + Filter module 下推完成）
> 目的：回答两类不同查询方向的问题——整体/全局问题用完整文档（全量池），细节/步骤问题用切片（切片池），两池独立索引、各召回一次、RRF 融合。
> 落地记录：`每模块流水线-tasks.md` 1.13/1.13.1/1.13.2/1.13.3；后端联调见 `后端联调对接清单.md`。

---

## 1. 背景与动机

原 RAG 是**单一索引** `rag-index`（324 块，全部是切片，2026-08-26 已清空）。面试问题分两类查询方向：

| 问题类型 | 示例 | 需要的数据形态 |
|---|---|---|
| **整体/全局** | "AI答疑是什么？" "架构怎么分工？" | **完整文档**（权威整篇，不丢上下文） |
| **细节/步骤** | "某一步怎么走？" "J1 这个坑根因？" | **切片**（细粒度块，精准命中） |

单一索引用切片块回答整体问题，会丢失整篇上下文；用完整文档回答细节问题又会太泛。**双池检索**让两池互补：全量池管整体、切片池管细节，各召一次再融合。

## 2. 用户决策（2026-08-26 确认 + 落地）

| 决策项 | 结论 |
|---|---|
| **全量池内容** | `1.语雀/`(5) + `4.完善文档/`(8) + `3.代码/`(10) 的**完整文档**，每篇一块 → **23 块**（索引 rag-full） |
| **切片池内容** | `docs/rag/ai-tutoring/切片数据/` 下 **294 块**（`md_to_jsonl.py` 从已切 md 生成，**不含完善文档**——与全量池整篇重复） |
| **索引隔离** | 两个独立索引 `rag-full` + `rag-slice`，同一 RAG 桶 `rag-1318177119` 下（已建好） |
| **召回融合** | 全量池召一次 + 切片池召一次 + BM25 → **三路 RRF 融合**取 Top-K |
| **category 筛选** | **已去掉**（2026-08-26 决策：LLM 判 9 类不准会误伤相关块，评测验证后移除） |
| **模块过滤** | 全量池/切片池都按 **module** Filter（COS 向量层下推）；intent 判模块，`current_project` 倾向约束 |

## 3. 数据流全景（已落地）

```
全量池源(语雀5+完善文档8+代码10) ─slice_full.py─▶ rag_slices_full.jsonl(23块, tags.pool=full)
切片池源(切片数据/ 294块)          ─md_to_jsonl.py─▶ rag_slices.jsonl(294块, tags.pool=slice)
                                                      │
                          build_index.py --pool full  │  build_index.py --pool slice
                                                      ▼
                                        rag-1318177119 / rag-full + rag-slice
                                                      │
查询: retrieve_dual(question) ──▶ rag-full 查一次 + rag-slice 查一次
                                                      │
                          orchestrate: 三路 RRF 融合(全量向量 + 切片向量 + BM25)
                                                      ▼
                                          generate(doubao)
```

## 4. 关键架构约束

1. **text 不进 metadata**（COS 向量索引 ~20KB/条限制）——text 在编排阶段按 `key` 从本地 jsonl 反查。
2. **key 是唯一 join key**：`_key_of` = `rag-{pool}/{file}/{anchor}#{idx}`（池前缀防两池冲突，已落地）。
3. `retrieve_vector(question, vector_type, module)` 支持双池 + 按 module Filter；`orchestrate` 三路 RRF（全量向量 + 切片向量 + BM25）。
4. `_resolve_bucket_index` 已改 `startswith("rag")`（rag/rag-full/rag-slice 全路由 RAG 桶）。

## 5. 设计细节（已落地，标注实现状态）

### 5.1 数据准备 ✅
- `scripts/rag/slice_full.py`：`1.语雀/` + `4.完善文档/` + `3.代码/分析-*.md` 整篇一块 → `rag_slices_full.jsonl`（23 块，tags.pool=full）
- `scripts/rag/md_to_jsonl.py`（新增，替代旧 324 块）：从 `切片数据/` 已切 md 生成 `rag_slices.jsonl`（**294 块**，tags.pool=slice，不含完善文档）
- summary/category 从 md 文件头解析；file_path = COS key

### 5.2 key 池命名空间 ✅
```python
# 全量池 key:  rag-full/{file}/{anchor}#{idx}
# 切片池 key:  rag-slice/{file}/{anchor}#{idx}
```
- `build_index.make_key(vector_type, file_, anchor, idx)`；`query._key_of(b)` 从 `tags.pool` 取前缀

### 5.3 索引写入 ✅
- `build_index.py --pool full|slice`（--clear 各清各的）；已入桶：rag-full 23 条 + rag-slice 294 条
- `settings.COS_VECTORS_INDEXES` 已加 `"rag-full"` / `"rag-slice"`

### 5.4 桶路由 ✅
- `_resolve_bucket_index` 已改 `vector_type.startswith("rag")`

### 5.5 双池召回 ✅
- `retrieve_vector(question, vector_type, module, categories)`：向量层 **module Filter**（categories 参数保留兼容不再过滤）
- `retrieve_dual(question, corpus, locked_categories)`：全量池/切片池都按 module 过滤 + BM25（模块过滤 pool）
- `orchestrate` 加 `vec2_result`：三路 RRF `1/(RRF_K + rank)`；**不做类别过滤/提权**（2026-08-26 决策）

### 5.6 调用方核对
- `query.py:352`、`api/rag.py:57/63`、`scripts/rag/rag_query.py:33/41`、`core/rag/assistant.py:76/111` 同步
- `generate` 层零改动（只消费 hit["text"]）

## 6. 涉及文件清单

| 文件 | 改动 |
|---|---|
| `scripts/rag/slice_full.py` | **新增**：全量池切片 → `rag_slices_full.jsonl` |
| `scripts/rag/build_index.py` | 多池写入 + make_key 加池前缀 + settings 索引名 |
| `config/settings.py` | `COS_VECTORS_INDEXES` 加 rag-full/rag-slice |
| `core/tutoring/vector_store.py` | `_resolve_bucket_index` 特判改 startswith("rag") |
| `core/rag/query.py` | `_key_of` 加池前缀；`retrieve_vector` 加参；`retrieve_dual`；`orchestrate` 加 vec2_result |
| `core/rag/assistant.py` | `recall` 双池 |
| `scripts/rag/rag_query.py` + `api/rag.py` | CLI/API 接入 |
| 测试 | `tests/rag/test_rag_query.py`、`tests/tutoring/unit/test_vector_store.py` 更新 + 新增双池测试 |

## 7. 验证方案

1. `slice_full.py` 跑通 → `rag_slices_full.jsonl` 23 块（tags.pool=full）✅
2. `md_to_jsonl.py` 从切片数据 md → `rag_slices.jsonl` 294 块（tags.pool=slice）✅
3. `build_index.py --pool full` + `--pool slice`（--clear）→ rag-full 23 条 + rag-slice 294 条（list_vectors 确认）✅
4. `rag_query.py "AI答疑是什么？" --no-gen` → top 命中全量池完善文档 01（整篇）；`"J1会话卡死"` → 命中切片池坑档案 J1 ✅
5. `pytest tests/rag tests/tutoring/unit/test_vector_store.py -v` 全绿 ✅
6. 多模块：`"RAG系统怎么做多路召回"` → 判 rag-system → 拒答（隔离正确）；`"项目开发遇到过哪些坑"` → 判 ai-tutoring → 命中坑档案 ✅
7. 评测（2026-08-26）：质量分 4.17，hit@3 0.50（边界拒答 1 条 + 数据关联跨模块查询待调）✅

## 8. 注意事项

- 索引 `rag-full`/`rag-slice` 已在控制台建好（768/float32/cosine），重建直接 `--clear`
- 重建前备份 jsonl；`slice_full.py` 从源文件生成会覆盖（完善文档 01 增强段已补回源文件）
- 旧 `rag-index`（单池 324）已清空（B3）
- **category 筛选已去掉**（2026-08-26 决策，见 1.13.2）；**module 过滤保留**
- 后端联调见 `后端联调对接清单.md`（U4 `/api/rag/source` 改读 COS 前置阻塞）

## 9. BM25 与双池的关系（2026-08-26 实测结论）

### 9.1 BM25 不能去掉——双池和 BM25 是正交的

**双池解决"存哪"（全量/切片），BM25 解决"多一路召回"**。双池后是三路 RRF 融合，BM25 作为独立关键词路继续保留：

```
查询 ──▶ 全量池向量(rag-full) ─┐
       ──▶ 切片池向量(rag-slice)─┼──▶ RRF 融合 ──▶ top-K
       ──▶ BM25(本地jsonl) ─────┘   ← 这路还在
```

**BM25 独特价值（向量替代不了）**：

| 场景 | 向量能否替代 | 为什么 BM25 有用 |
|---|---|---|
| 专业术语/代码名（decide / SseMasterySignalDTO / 表名） | ⚠️ 可能 miss | 精确关键词命中 |
| 短问题精确匹配（"J1 会话卡死"） | ⚠️ 语义漂移 | 精确命中坑编号 |
| 向量召回质量差时兜底 | ❌ | 第二路独立信号 |
| COS 向量桶挂了（断网） | ❌ 全断 | **BM25 纯本地，可降级查询** |

**结论**：双池把向量路从 1 路变 2 路，BM25 是第 3 路，三路互补都要留。去掉 BM25 会掉精确召回（术语/编号）。等块数上千 BM25 变慢时，优化方向是**缓存倒排索引**或**分模块独立 BM25**，而不是去掉。

### 9.2 只 BM25 summary 的权衡（实测旧 324 块语料，当前 294 块参考）

| 方案 | 耗时 | 加速比 | Top-10 命中重合 |
|---|---|---|---|
| 全量（text+summary） | 170 ms | 1× | — |
| **只 summary** | 9.6 ms | **~18× 快** | **7/10** |

**结论：不建议只 BM25 summary**：
- summary 是 LLM 生成的一句话（几十字），信息密度低
- 精确关键词（decide / 坑编号 / 表名）多在 **text 正文**，不在 summary → 漏 30% 精确召回
- 当前 170ms 相对 COS 网络 + LLM 生成**不构成瓶颈**，为省 160ms 牺牲 30% 召回不划算

**正确优化路径**（块数上千、真慢时）：缓存 BM25 倒排索引（块不变复用，查询只打分）→ 保留全 text；或 `select_corpus` 按模块过滤只打目标模块池。
