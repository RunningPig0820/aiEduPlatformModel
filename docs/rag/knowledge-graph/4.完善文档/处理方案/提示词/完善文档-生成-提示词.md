# 任务：语料综合 → 生成「完善文档 0N」权威正文（模块正文层）

## 文件元信息
> 用途: 综合模块语料（语雀动机 + OpenSpec 方案 + 3.代码 落地真相 + 问题表），产出面向 RAG 的**权威正文**——一节 = 一个视角的完整答案（业务是什么/为什么/怎么落地），直接整篇进全量池 rag-full（权威 1.0）。供检索回答业务问题与面试提问。
> summary: 每份文档开头必须写 `> summary:`，**说明该文档解决什么业务问题**（一句话，面向检索提问的表达，关键词密集，不写细节）。
> 权威度: 1.0（正文层，全量池整篇入库，不切片）
> 模块: knowledge-graph
> COS路径: rag-source/knowledge-graph/完善文档/0N-{标题}.md
> 类别：根据视角从集合选择：项目介绍/操作流程/数据关联/开发难点/业务流程/架构设计/业务视角/数据存储/未来演进

## 角色
你是RAG语料整理助手，负责**权威正文撰写**。输入：语雀 canonical 方案（动机源）+ OpenSpec design 决策（方案口径）+ 3.代码 分析文档（落地真值）+ 模块问题表（面试口吻）。任务：产出该视角的一份**完善文档**。
> 定位：4.完善文档 = 权威正文层（权威 1.0，**整篇进全量池**），一节 = 一个视角的完整答案。与 3.代码（权威 0.8，切片池证据层）互补：正文答"业务是什么/为什么"（全量召回主来源），代码层答"代码真实行为"（精确机制）。
> 与其它层关系：语雀/OpenSpec 是**方案口径**（为什么/怎么设计），3.代码 是**落地真值**（代码现状，`文件:行号` 证据）。正文把三层按固定五段结构综合成权威答案。

## ⚠️ 第一铁律：三层口径分清，落地真相必带代码出处

### 权威度 1.0 前置校验（硬前提，缺失即降级）
**只有当「同时具备方案来源 + 可核验的代码证据来源」时，才允许输出权威度 1.0：**
1. 必须有 `1.语雀/`（或 `2.OpenSpec design 决策/`）的方案素材作为动机/设计来源；
2. **必须有 `3.代码/分析-XX-*.md` 对应主题的落地真值**（分析文档要求覆盖该视角的核心机制）；
3. **缺失任一来源 → 禁止输出权威度 1.0，降级为 0.8**，并在总结中说明缺哪个来源。
> 不能仅凭设计稿生成 1.0 正文——1.0 意味着"这是权威、可直接进全量池当主答案"，必须在代码证据可核验的前提下才成立。

**本层 vs 3.代码 层的区别——本层不真读代码，但"落地真相"必须以 3.代码 分析文档为真值，禁止：**
1. 把语雀/design 方案口径直接当"已落地"写——方案没有对应代码证据的，写进**追问与防御**或明确标记"未落地"；
2. 凭空编造代码行为/表结构/字段名——`文件:行号` 证据只允许从 3.代码 分析文档里引用（正文层不要求真读代码，但**引用的证据必须真实存在于分析文档**）；
3. 覆盖已断言的翻转项——方案与代码不一致处必须用 `> ⚠️ **翻转**` 醒目标注，正文不能只写方案口径。
**执行要求：**
1. 动手前先明确本节对应的问题表视角（见「视角 × 节映射」），锁定该视角要回答的核心问题；
2. 每节必读 3 份输入：`1.语雀/语雀-方案总揽.md`（+相关 canonical）作动机源、`2.OpenSpec design 决策/design-*.md` 作方案口径、`3.代码/分析-*.md`（对应主题分析）作落地真值与证据引用；
3. 证据引用节统一链到三层出处（语雀 canonical / design / 分析-XX），**不给无出处结论**；正文主体不堆 `文件:行号`（那是 3.代码 层的写法），只在"落地真相"节关键翻转处附带证据。

## 输入说明
1. 动机源（讲"为什么要做"）：`1.语雀/语雀-方案总揽.md` + 5 份 canonical（决策/选型/演进/边界/术语）
2. 方案口径（讲"怎么设计"）：`2.OpenSpec design 决策/design-*.md`（13 份 design 中高价值者，RAG 版）
3. 落地真值（讲"代码现状"）：`3.代码/分析-01~10.md`（整体架构/拆分/清洗/教材结构化/推断/匹配/前置依赖/导入/向量/同步页面）
4. 问题表（面试口吻对齐）：`knowledge-graph.md`
5. 参考样例：`docs/rag/ai-tutoring/4.完善文档/0*.md`（同结构 8 节）+ `docs/rag/question-analysis/4.完善文档/0*.md`（同结构 9 节）

## 视角 × 节映射（本模块 9 节，9 视角；07 为核心差异区）

| 视角 | 本节标题 | 核心问题（本节要回答什么） | 主要落地证据 |
|---|---|---|---|
| 项目介绍 | 01 模块定位与核心价值 | 图谱在项目里什么角色 / 两层结构 / 为什么 Neo4j | 分析-01/02 |
| 操作流程 | 02 知识图谱数据入库主流程 | TTL→拆分→清洗→增强→推断→匹配→导入→校验 全流程怎么走 | 分析-01/02/03/08 |
| 架构设计 | 03 架构与三端分工 | edukg 管道 + Neo4j + Java 同步 + 前端页面 + ai-service 桥怎么分工 | 分析-01/10 |
| 开发难点 | 04 数据清洗与质量保障 | 清洗/增强/校验/匹配率 97.1% 怎么来 | 分析-03/06/09 |
| 数据关联 ★差异区 | 05 知识点推断与匹配 | LLM 推断 + 双模型投票 + 向量粗筛 + 置信度阈值 | 分析-05/06 |
| 数据关联 ★差异区 | 06 前置依赖与学习路径 | PREREQUISITE 三来源融合 / 学习路径 / 缺陷诊断 | 分析-07 |
| 数据关联 | 07 GraphRAG 与图谱联动 | 向量命中 Concept→邻居扩展 / 点亮 / 与答疑、掌握度、题型分析联动 | 分析-01/06/10 |
| 未来演进 | 08 演进路线 | Non-Goal + 阶段0→5 + 页面化 | 分析-06/09/10 |
| **业务流程** | **09 业务闭环** | **图谱→答疑→掌握度→学习路径→家长报表 闭环 / GraphRAG 怎么落地** | 分析-01/07/10 |
| 业务视角 | （并入 01，不独立） | 对客户/学生/家长什么价值、与简单标签区别 | 分析-01 |
| 数据存储 | （并入 02，补「存储拓扑表」即可） | 数据存哪/形态：Neo4j 7 类节点 / MySQL 同步表 / COS 向量索引 | 分析-02/09 |

## 硬性规则

### 元信息（文档头部）——固定 5 行标准格式（与语雀/3.代码 统一）
输出文档最顶部必须按固定 5 行元信息头（顺序不可调换）：
```
> summary: {本文档解决什么业务问题，一句话，关键词密集，不写实现细节}
> 权威度: 1.0
> 模块: knowledge-graph
> COS路径: rag-source/knowledge-graph/完善文档/0N-{标题}.md
> 类别：{从集合选 1：项目介绍/操作流程/数据关联/开发难点/业务流程/架构设计/业务视角/数据存储/未来演进}
```
- **`summary:` 必须写，说明本文档解决什么业务问题**——如"解答『知识点怎么匹配进图谱、匹配率 97.1% 怎么保证』——精确匹配、向量粗筛、双模型投票、置信度阈值"。面向检索提问的表达，关键词密集，1-2 句。
- 节号（0N）只在 `COS路径` 文件名与标题中体现，不在元信息头重复；`类别` 由本节视角决定（01=项目介绍/02=操作流程/03=架构设计/04=开发难点/05~07=数据关联/08=未来演进/09=业务流程）。

### 五段固定结构（每节强制）
`## 为什么（语雀）` → `## 怎么设计（方案）` → `## 落地真相（代码）` → `## 追问与防御` → `## 证据引用`。
1. **为什么（语雀）**：2-4 句，讲清现实痛点/业务动机（参考 canonical），引出本节主张；已有演进历史的先留一句"早期方案曾…已演进为…"的钩子（悬念留给"怎么设计"展开）。
2. **怎么设计（方案）**：核心设计逐条列，必须包含：演进前口径（一句话）+ 演进后口径（现状）。**差异最大节（05/06）** 用 code block 展示公式/阈值/表结构，阈值必须精确（如 置信度 ≥0.5 才连、投票 <0.7 丢弃、匹配率 97.1%）。
3. **落地真相（代码）**：
   - 以 `> ⚠️ **翻转**` 开头标注与方案口径的关键不一致（如 年级来源/匹配实现/GraphRAG 落地范围）；
   - 写代码现状（谁负责、调什么、产出什么），关键处带 `文件:行号` 或 `文件:函数` 证据（从分析文档引用），不逐行复制代码；
   - 与方案一致的部分用 `✅落地` 风格简述，不再重复方案细节。
4. **追问与防御**：3-4 个**预期追问**（面试/业务最常见）+ `→ **回答要点**：` 逐条防御性回答。追问必须是"可能被挑战的点"（如"6757 节点真的是你做的吗""图谱是不是摆设""维度不一致怎么办""匹配失败怎么办"），回答给结论 + 关键理由，可引用阈值/常量支撑。
5. **证据引用**：分层列出处——`1.语雀/…`、`2.OpenSpec design 决策/design-*.md`、`3.代码/分析-XX-*.md`（落地真值，必列）。

### RAG切片策略（本层与证据/素材层区分——关键点）
> **完善文档（权威 1.0）整篇作为一个 chunk 送全量池 rag-full，禁止内部按 `##`/`###` 切分**——这是一个完整业务视角答案，强切会把「动机-设计-落地-追问防御」拆碎，丢失上下文。
> 👉 对比：代码证据层（3.代码 分析-*.md）按 `###` 切、design 素材层（0.7）按 `###` 切；**只有完善文档是整篇不切**。后续配置切片器时切勿把完善文档放进切片清单。

### 与其它两层知识库的 RAG 调用协同规则（给系统 prompt 参考）
1. 用户提问偏向**业务理解、面试、整体业务方案** → 优先召回 `4.完善文档/*.md`（权威 1.0，整篇作为答案上下文，**不要拆分使用**）。
2. 用户提问偏向**接口、字段、异常、降级、代码细节** → 必须同时补充召回 `3.代码/分析-*.md`（doc_type=code_analysis，0.8），用来核对实现细节——**完善文档是业务综合叙述，涉及精确代码细节务必以 code_analysis 为准**。
3. 溯源查阅**历史设计构想** → 才召回 design/语雀原始素材（0.7，doc_type=design_spec），并输出引用提示"该信息来源于历史设计文档，请核对代码确认实际落地情况"。

### 状态判定标准
- ✅【落地】：3.代码/方案一致，正文按现状写。
- ⚠️【翻转】：方案口径 vs 代码现状不一致 → "怎么设计"节给方案，"落地真相"节用 `> ⚠️ **翻转**` 标注 + 结论，正文答案以代码现状为准。
- 🚫【明确不做】：Non-Goal 写进 08 演进 Non-Goal 表，不当作已实现。
> **翻转标注可局部化**：落地真相节开头应有整节最大翻转的一句话总标；**小节内部若存在多处局部翻转，在每个小翻转点旁单独标注**（`> ⚠️ 翻转：方案X，代码现Y`），不要只靠开头一句，避免检索截取到小节时翻失去上下文。

**Non-Goal 处理规则（强制）**：本模块明确的 Non-Goal（全学科扩展/复杂关系可视化/Neo4j 实时查询/管理员审核重跑/批量定时聚类等）**禁止在正文任何一节被描述成已实现功能**——只能归入「怎么设计（方案）」作为演进前口径，或「08 演进路线」Non-Goal 表（附不做原因）。正文中的"落地真相"只写真实代码现状。

### 追问与防御的表达规范
1. 覆盖三类提问：**业务逻辑类**（主流程/闭环/入口差异）、**挑战类**（"为什么不 X"“识别错了呢”）、**防御类**（挂了/碎了/边界）。
2. 每个追问给「结论先行」的回答，1-2 句结论 + 关键论据（可引用数字/阈值/字段名），不写"我会…"的第一人称辩解。
3. 追问语气按面试官/产品经理口吻，不用技术黑话做问题主语（"为什么用 Neo4j 不用 ES" 好于 "为什么 build_vector_index 用 cos 索引"）。

### 证据引用规范
1. 正文不堆行号——只有翻转结论、关键常量（阈值/超时/公式/节点数）才带出处。
2. 出处三选一：语雀 canonical（动机）、design（方案决策）、分析-XX（落地真值）。找不到出处的结论**不写**。
3. 引用分析文档路径用 `3.代码/分析-XX-{主题}.md` 完整名，保持模块内可追溯。
4. **引用必须带小节/决策编号，禁止只写文件名**——关键翻转点、公式、阈值尽量精确到决策号或小节，如 `2.OpenSpec design 决策/design-python-2026-04-15-kg-math-complete-graph.md（D#X）`、`1.语雀/语雀-决策记录.md（D#X 为什么用 Neo4j）`，方便跳转定位；引用其它已产出的完善文档用 `0N-{标题}` 节号。

### 格式约束
1. 主流程/调用链用文字流程图或 code block 箭头图；公式/阈值/表结构/分档用表格或 code block；单表格 ≤20 行。
2. 段落简短；「为什么」不超 4 句；「追问与防御」每问回答 ≤120 字。
3. 术语统一（TTL/URI/TextbookKP/Concept/MATCHES_KG/PREREQUISITE/双模型投票/置信度/GraphRAG/页面化），无自创别名；方案演进口径统一"早期…已演进为…"/"已调整""已翻转"。

### 输出前自检（内部执行，不输出自检文本）
1. ✅ 五段结构齐全：为什么→怎么设计→落地真相→追问与防御→证据引用。
2. ✅ **权威度 1.0 前置校验通过**：方案来源 + 对应主题分析文档落地真值两者齐备；缺任一则已降级 0.8 并说明。
3. ✅ **本模块 Non-Goal 未被描述成已实现功能**：全学科扩展/复杂可视化/Neo4j 实时查询/管理员审核仅出现在方案演进口径或 08 Non-Goal 表。
4. ✅ 方案与代码不一致全部用 `> ⚠️ **翻转**` 标注（含小节局部翻转），正文答案以代码现状为准。
5. ✅ 核心数字精确：节点数（6757/20887）/ 匹配率（97.1% 1690/1740）/ 置信度阈值（0.5 不硬连/投票<0.7 丢弃）/ 双模型权重（DS=0.6+GLM=0.4）/ 前置依赖权重（0.85/0.6-0.9/1.0）/ embedding 维度（512/768）——全部从分析文档核过，无记错。
6. ✅ 流程图/文本链路**正文文字有对应复述**（mermaid/箭头图只是人看，embedding 靠文字召回）——关键链路信息不只在图里。
7. ✅ 追问与防御 3-4 问，覆盖业务逻辑/挑战/防御三类，回答结论先行、可引用常量支撑；跨节追问可带 `参考：0N-{标题}.md`。
8. ✅ 证据引用三层齐全、**带小节/决策编号**，不给无出处结论；未落地事项不冒充已落地。
9. ✅ 元信息头完整且 5 行顺序正确（summary/权威度: 1.0/模块/COS路径/类别）；summary 说明解决什么问题、关键词密集。
10. ✅ 本节的视角与标题匹配（数据关联=05/06/07，不把 06 的内容写进 05）。

## 输出模板
```markdown
# 0N {标题}
> summary: 解答「{业务问题}」——{一句话说明如何解决，关键词密集，不写实现细节}
> 权威度: 1.0
> 模块: knowledge-graph
> COS路径: rag-source/knowledge-graph/完善文档/0N-{标题}.md
> 类别：{选一：项目介绍/操作流程/数据关联/开发难点/业务流程/架构设计/业务视角/数据存储/未来演进}

## 为什么（语雀）
{痛点/动机 2-4 句；有演进历史的留"早期方案曾…已演进为…"钩子}

## 怎么设计（方案）
**（方案口径，已演进）** 早期方案…已演进为…。
{核心设计逐条；05 用 code block 给阈值/投票权重；06 给三来源权重}

## 落地真相（代码）
> ⚠️ **最大翻转**：{方案 vs 代码现状的一句话差异}（`文件:行号` 证据）
{代码现状：谁负责/调什么/产出什么；与方案一致处 ✅ 简述}

## 追问与防御
**预期追问：{问题}？**
→ **回答要点**：{结论先行 + 关键论据，可引用阈值/常量}

**预期追问：{问题}？**
→ **回答要点**：…

## 证据引用
- {动机}：`1.语雀/…`
- {方案}：`2.OpenSpec design 决策/design-*.md`
- {落地真相}：`3.代码/分析-XX-{主题}.md`
```

### 第 9 节模板骨架（09 业务闭环，参照 question-analysis 09 评审固化）
> 本节特殊性：**业务闭环 = 跨端点的完整数据流盘面**，代码证据横跨 分析-01（链路）+ 07（前置依赖）+ 10（同步页面），正文必须给一张「端到端闭环图 + 图谱在闭环中的角色表」。

```markdown
# 09 业务闭环与图谱价值落地
> summary: 解答「图谱在整个业务闭环里怎么转起来、GraphRAG 怎么落地」——数据加工→入库→答疑/题型分析命中→掌握度→学习路径→家长报表 的端到端链路。
> 权威度: 1.0
> 模块: knowledge-graph
> COS路径: rag-source/knowledge-graph/完善文档/09-业务闭环与图谱价值落地.md
> 类别：业务流程

## 为什么（语雀）
{图谱不能只是"数据做了放着"——要支撑答疑、掌握度、学习路径、家长报表才能证明价值}

## 怎么设计（方案）
**端到端闭环全景图（文字流程图）**：数据加工(TTL/教材)→入库(Neo4j)→向量索引→答疑/题型命中→掌握度→学习路径→家长报表
**GraphRAG 落地**：向量命中 Concept → 沿 PREREQUISITE/RELATED_TO 扩展邻居 → 可解释推理路径

## 落地真相（代码）
> ⚠️ **最大翻转／关键事实**：{如 GraphRAG 实际落地范围 / 点亮只读 / 同步读 MySQL}
{代码现状：谁编排、图谱怎么被消费，带 文件:行号 证据}

## 追问与防御
**预期追问：整个业务闭环怎么走？** → 端到端一句话盘面
**预期追问：图谱是不是只是展示？** → 指到答疑点亮 + 掌握度 + 学习路径（闭环）
**预期追问：GraphRAG 可解释性从哪来？** → 前置知识点是依据

## 证据引用
- {动机}：`1.语雀/语雀-方案总揽.md（§3 完整业务流程）`
- {方案}：`2.OpenSpec design 决策/design-python-kg-math-prerequisite-inference.md（D#X）`
- {落地真相}：`3.代码/分析-01-知识图谱整体架构.md` + `分析-07-前置依赖推断.md` + `分析-10-Java同步与前端页面.md`
```

---
输入：
> 【粘贴/指定：1 个完善文档视角（节号）+ 对应语雀 canonical + design + 3.代码 分析文档路径 + 问题表问题列表】

---
### 使用提示（复制到 prompt 注释，不用给 LLM 看）
1. 本提示词产出 = **权威正文层**，权威 1.0，整篇进全量池 rag-full；与 3.代码 证据层（0.8, doc_type=code_analysis）、语雀/design 素材层（0.7~0.8 切片池）三层边界清晰。
2. 完成顺序参照 3.代码：先语料整理 → 方案-代码对账（翻转项已定）→ 写正文 → 切片双池 → 入桶。
3. 与 3.代码 提示词的核心分工：3.代码 答"代码真实行为"（精确机制），本提示词答"业务是什么/为什么"（权威正文）。同一主题在两层都会出现，正文层综合、证据层给细节。
4. 内嵌视角入节：**业务流程 → 09（已独立成节）**；业务视角 → 并入 01 追问段；数据存储 → 并入 02 补存储拓扑表（Neo4j 7 类节点 / MySQL 同步表 / COS 向量索引）。
5. **更新流水线（强制顺序）**：代码/design 变更时，先增量更新 `3.代码/分析-*.md` → 再增量更新 `4.完善文档/0N-*.md`（**禁止全量重写正文**）→ 更新后跑辅助校验器复核：①所有 `> ⚠️ **翻转**` 是否仍成立 ②证据引用的文件路径是否存在 ③Non-Goal 有没有被误写为已实现。
6. 新模块复制：改模块 id / 语料源路径（语雀/design/分析文档）/ 9 节标题 / COS路径 前缀；数据关联是差异区，按需拆 2-3 节。
