# 管理端审核队列本期不做

> summary: 管理端审核队列本期前端不做（后端接口已就绪），后续挂 /admin/knowledge-graph 图谱页，教师端同理不做。
> 权威度: 0.7
> 模块: knowledge-graph
> COS路径: rag-slices/knowledge-graph/OpenSpec/design-frontend-kp-matching-lightup-frontend-D8-管理端审核队列本期不做.md
> 类别：未来演进

---

### 8. 管理端审核队列本期不做（后端接口已就绪，后续挂图谱页）

> 检索摘要：管理端审核队列本期前端不做（后端接口已就绪），后续挂 /admin/knowledge-graph 图谱页，教师端同理不做。

后端已确认管理端审核接口（`GET/POST /api/kg/aliases/pending`）本期前端不做。核心闭环是学生端掌握度展示 + 题型分析 + 澄清，管理端「极少数边界仲裁」队列稀疏、可后续再挂到 `/admin/knowledge-graph` 页。教师端同理本期不做。

> 证据：详见 `2.OpenSpec design 决策/design-frontend-kp-matching-lightup-frontend.md`（§8. 管理端审核队列本期不做（后端接口已就绪，后续挂图谱页））
