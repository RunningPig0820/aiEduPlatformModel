# 阶段三: TTL 生成（可选）
> summary: 阶段三整合教材与课标知识点生成 TTL 格式文件，人工确认后手动导入 Neo4j。
> 权威度: 0.7
> 模块: knowledge-graph
> COS路径: rag-slices/knowledge-graph/OpenSpec/python-2026-04-10-textbook-concept-linking-阶段三-ttl生成.md
> 类别：操作流程

> 检索摘要：阶段三整合教材与课标知识点生成 TTL 格式文件，人工确认后手动导入 Neo4j。

```bash
1. 整合教材 + 课标知识点
2. 生成 TTL 格式文件
3. 人工确认后手动导入 Neo4j
```

> 证据：详见 `2.OpenSpec design 决策/design-python-2026-04-10-textbook-concept-linking.md`（§阶段三: TTL 生成（可选））
