# 切片数据

> 语料切片的**人可读视图**。切片方式：提示词 + 大模型语义分组（见各来源 处理方案）。
> 状态：2026-08-29 **5 来源切片完成（306 块）**。代码/坑/引导按来源语义切；**语雀/OpenSpec 按业务主题合并**（`业务主题清单.md` 17 主题，非按文档结构 D#/### 拆——业务匹配，不是框架结构）。
> 大小约束：每块正文 ≤5000 字符（全文向量化入桶，embed 输入上限），超长拆 -2/-3。

| 来源 | 权威度 | 切片数 | 说明 |
|---|---|---|---|
| 代码（开发+面试双切片） | 0.8 | 71 | `代码/切片/` 11 份分析语义分组（开发向:业务链路/代码事实/坑与对账 + 面试向:业务与流程/架构与技术取舍/落地问题与复盘） |
| 坑档案（开发+面试双切片） | 0.8 | 24 | `坑档案/切片/` J-KG1~12 一坑一文件双切片，6 段原文 + 双证据 |
| 引导问题（问答切片） | 1.0 | 64 | `引导问题/切片/` 问题列表 64 问一问题一文件（9 视角），权威 1.0 合成问答 |
| 语雀（canonical） | 0.8 | 56 | `语雀/切片/` **按业务主题合并**（`业务主题清单.md` 17 主题，9 份 canonical → 56 块，超长拆 -2/-3，2026-08-29 重切） |
| OpenSpec（design 素材） | 0.7 | 91 | `OpenSpec/切片/` **按业务主题合并**（12 份 RAG 版 → 91 块，含新主题 18~22，正文检索摘要必带，2026-08-29 重切） |
| **合计** | | **306** | |

> 各来源目录 = `readme.md（来源切片说明）` + `处理方案/（处理路径 + 提示词 + 专项切片）`，参考 `1.语雀/处理方案/` 结构。
> 切分边界/口径以 `../切片清单.md` 为准。
> 模块化：所有切片 COS路径 前缀 `rag-slices/knowledge-graph/...`，与 ai-tutoring / question-analysis 共用 rag-slice 索引，靠 metadata.module 过滤隔离。
