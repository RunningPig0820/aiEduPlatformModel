# 坑档案-J-KG10-下钻慢SQL-复盘

> summary: 下钻慢SQL复盘
> 来源: 切片 ｜ 锚点: 坑点复盘与口述
> 节: 坑档案 J-KG10 下钻慢SQL
> COS路径: rag-slices/knowledge-graph/坑档案/坑档案-J-KG10-下钻慢SQL-复盘.md
> 类别：开发难点
> target: 面试项目问答

---

## 坑点复盘
**现象**：知识图谱总览接口在腾讯 MySQL 慢 SQL 报告中被标记——7 表 JOIN 分页，请求响应慢、DB 压力大。

**触发链路**：前端打开知识图谱总览 → 一次拉全学段→年级→课本→章节→小节→知识点全量 → 做 7 表 JOIN + 分页。

**根因**：7 表 JOIN + GROUP BY 先于 LIMIT + COUNT DISTINCT 全扫 + stage 字段无索引 + 每 JOIN 带 is_deleted 条件——一次性把整棵教材树拼出来分页，等价于全表扫描。

**解决思路与权衡**：改成点击式下钻：学段→年级→课本→章节→小节→知识点逐层查询，每层单表/2 表 JOIN、数据量小无需分页；删除 7 JOIN 分页端点，前端同步改成菜单式下钻 5 层 GET。

## 面试口述要点
树形/层级数据一次拼全量是大忌——每层数据量小、用户是逐步下钻的，改成"点哪层查哪层"就把 7 JOIN 全扫拆成 N 个索引单查。慢 SQL 优化先看"是不是查了用户不需要的量"，再看索引；COUNT DISTINCT 全扫 + GROUP BY 先于 LIMIT 是分页场景典型反模式。

> 证据：详见 `5.难点/坑档案.md`（J-KG10）｜ `3.代码/分析-11-Java同步与前端页面.md`
