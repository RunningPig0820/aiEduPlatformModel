# 第一道解析与生成

> summary: 数据第一道防线是什么？把 23 册教材 JSON 和 TTL 解析成标准节点/关系结构——TTL 用 rdflib 自动解码 Unicode，教材 JSON 生成 Textbook/Chapter/Section 节点和包含关系。
> 权威度: 0.8
> 模块: knowledge-graph
> COS路径: rag-slices/knowledge-graph/语雀/语雀-数据源与数据源清理-二-1-解析与生成.md
> 类别：操作流程

---

### 第一道：解析与生成——把原始素材变成结构化数据
> 检索摘要：数据第一道防线是什么？把 23 册教材 JSON 和 TTL 解析成标准节点/关系结构——TTL 用 rdflib 自动解码 Unicode，教材 JSON 生成 Textbook/Chapter/Section 节点和包含关系。

原始数据进来第一件事是**解析成统一结构**：

- TTL 用 rdflib 解析，自动处理 Unicode 解码（`一` → 中文）
- 教材 JSON 生成标准的 Textbook/Chapter/Section/TextbookKP 节点 + 层级包含关系

这一步不做任何判断，只是把"原始素材"变成"统一格式"，为后面清洗打底。

> 证据：详见 `1.语雀/语雀-数据源与数据源清理.md`（§二·第一道）
