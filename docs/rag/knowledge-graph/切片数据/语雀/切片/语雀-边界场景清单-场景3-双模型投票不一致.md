# 场景3 双模型投票不一致
> summary: DeepSeek 和 GLM 判断不一致怎么办？加权投票 DS=0.6/GLM=0.4，仅 DeepSeek=True 才可能过阈值（DS 否决），不一致时置信 ×0.7。
> 权威度: 0.8
> 模块: knowledge-graph
> COS路径: rag-slices/knowledge-graph/语雀/语雀-边界场景清单-场景3-双模型投票不一致.md
> 类别：开发难点

| 属性 | 内容 |
|---|---|
| 触发条件 | 匹配/前置推断时两模型结论不一致 |
| 当前处理 | 两模型一致 → 平均置信采纳；不一致 → 加权投票，DS 否决权，置信 ×0.7 |
| 兜底 | 前置投票不一致 → 不采纳；匹配不一致 → 走候选/人工 |
| 风险 | 系统性偏见双模型同向错 |
| 证据 | 证据：edukg/core/llm_inference/dual_model_voter.py:354-396 |

> 证据：详见 `1.语雀/语雀-边界场景清单.md`（场景3）
