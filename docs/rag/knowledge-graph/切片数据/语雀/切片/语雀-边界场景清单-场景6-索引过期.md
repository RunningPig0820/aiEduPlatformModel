# 场景6 索引过期（数据改了）
> summary: 图谱数据改了向量索引怎么办？checksum MD5 存 index_meta，失配强制重建索引，保证向量与图谱一致。
> 权威度: 0.8
> 模块: knowledge-graph
> COS路径: rag-slices/knowledge-graph/语雀/语雀-边界场景清单-场景6-索引过期.md
> 类别：开发难点

| 属性 | 内容 |
|---|---|
| 触发条件 | Neo4j 数据变更后向量索引未同步 |
| 当前处理 | `neo4j_checksum`（MD5 按 uri+label 排序拼接）校验 `is_index_valid`；失配回退懒加载重建 |
| 兜底 | 预构建索引 + 懒加载双轨 |
| 风险 | 索引重建需 60s |
| 证据 | 证据：edukg/core/textbook/vector_index_manager.py:204-246 |

> 证据：详见 `1.语雀/语雀-边界场景清单.md`（场景6）
