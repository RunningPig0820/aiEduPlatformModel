# 场景9 页面化同步冲突/脏数据
> summary: Neo4j→MySQL 同步失败或冲突怎么办？按年级拆分子任务 + 细粒度锁，失败仅影响该年级；URI 校验拦截非法 + 对账校验 reconciliation。
> 权威度: 0.8
> 模块: knowledge-graph
> COS路径: rag-slices/knowledge-graph/语雀/语雀-边界场景清单-场景9-页面化同步冲突.md
> 类别：操作流程

| 属性 | 内容 |
|---|---|
| 触发条件 | 同步任务并发、URI 脏数据、同步后不一致 |
| 当前处理 | 细粒度锁 `...:{edition}:{subject}:{stage}:{grade}`；URI 校验（edukg.org/knowledge/ 开头）；对账校验 reconciliation_status；MERGE/UPSERT 幂等 |
| 兜底 | 失败仅影响该年级；手动按需重触发 |
| 风险 | 状态膨胀（定期清理）；关联表重建开销（DELETE+INSERT） |
| 证据 | 证据：语雀-联调-知识图谱同步接口.md / 语雀-页面化-联调问题.md |

> 证据：详见 `1.语雀/语雀-边界场景清单.md`（场景9）
