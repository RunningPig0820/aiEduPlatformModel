# 数据加工流水线

> summary: 数据加工流水线
> 权威度: 0.8
> 模块: knowledge-graph
> COS路径: rag-slices/knowledge-graph/语雀/课标到图谱的构建链路-04-数据加工流水线.md
> 类别：操作流程

## 课标到图谱不是一步到位：八步流水线

从课标到图谱是 **8 步流水线**，每一步一个模块，模块清单明确标"是否用 LLM"：

| 步骤 | 模块 | 是否用 LLM |
|---|---|---|
| ① OCR | 扫描版 PDF → 文本（百度 OCR，收费） | 否 |
| ② 知识点提取 | 从课标文本提取教学知识点（434 个） | 是（glm-4-flash 免费） |
| ③ 教学知识点解析 | "复合体"拆核心概念 + 属性 | 否（规则） |
| ④ 概念聚合 | 去重合并 → 122 个概念 | 部分 |
| ⑤ Class 推断 | 概念归类到 Class（19 匹配 + 1 新建"数学符号"） | 部分 |
| ⑥ Statement 生成 | 每个概念生成定义（122 条） | 是（已缓存） |
| ⑦ 关系提取 | 312 条关系（relatedTo/partOf/belongsTo/HAS_TYPE/SUB_CLASS_OF） | 规则 vs LLM 分流 |
| ⑧ 校验导入 | 6 类本地校验 + 3 类 DB 冲突校验 → Neo4j | 否 |

> 一句话面试答案："课标变图谱走 8 步流水线——OCR→知识点提取→教学知识点解析→概念聚合→Class 推断→Statement 生成→关系提取→校验导入，每步一模块。"

### 成本驱动架构：贵的跑一次、便宜的随便跑

流水线成本选型贯彻"贵的跑一次、便宜的随便跑"：

- **百度 OCR**：收费，但**一次性执行**（189 页扫描版 PDF 识别一次就完事）
- **glm-4-flash**：免费，后续所有 LLM 步骤（知识点提取/定义生成/关系分析）都用它

OCR 是唯一花钱的环节，其余全流程用免费模型，整体成本可控。

> 一句话面试答案："OCR 收费但只跑一次，LLM 免费所以全流程随便用——成本选型就是'贵的跑一次、便宜的随便跑'。"

### 实现位置

完整 8 步流程与代码：`edukg/core/curriculum/`（README.md 693 行 + MAPPING.md 255 行）。数据文件：`edukg/data/eduBureau/math/`（课标 OCR 结果 + 概念/定义/关系 JSON）。
