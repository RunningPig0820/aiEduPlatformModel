# 切片数据 / 语雀 处理方案

1. **源头就绪**：`1.语雀/` 6 份 canonical（方案总揽/决策记录/方案选型对比/演进时间线/边界场景清单/术语对照表），由 `1.语雀/处理方案/提示词/` 产出
2. **按条目切片**：`提示词/语雀-切片-提示词.md` —— 决策 D#/选型#/演进阶段#/场景# 各一条一文件，方案总揽按 h2 主题，术语表整表一块；一个 chunk = 一条决策/选型/场景/阶段
3. **原文保真**：条目正文/属性表/mermaid 按原文保留，不转述不改写；只删 文档说明/全量总索引表/附录
4. **头部精简 5 行**：summary（源检索摘要完整保留）/ 权威度 0.8 / 模块 / COS路径 / 类别（9 视角闭集），无 `> 状态`；机器元信息（entry_id/source_doc）摄入时推导
5. **冲突校验**：与 方案-代码对账/坑档案 已记录 ⚠️翻转 矛盾时头部加 `> WARNING`（正文不篡改）
6. 入桶：COS路径 `rag-slices/knowledge-graph/语雀/...`，`doc_type=canonical`，权威 0.8

> 与 question-analysis 同构：语雀层按条目切片（决策/选型/场景/演进各一条一文件，术语表整表一块）用 `语雀-切片-提示词.md`（改模块 id / 源文档条目表 / 类别映射表）。
