# 4.完善文档 处理方案

> 状态：2026-08-27 **9 节定稿**（补 09 业务闭环与两域解耦）——评审确认整套体系逻辑完整、分层合理、可投入使用；提示词并入了 6 点评审优化（1.0 前置校验/Non-Goal 自检/RAG 协同/整篇禁切/09 骨架/证据带决策号）。

## 一、完善文档定位

**权威正文层（权威度 1.0，直接整篇进全量池 rag-full）**——一节 = 一个视角的完整答案（问题表入口），**整篇作为一个 chunk，禁止内部切片**。与 `3.代码/`（证据层，切片池 doc_type=code_analysis）互补：完善文档答"业务是什么/为什么"（权威正文），代码层答"代码真实行为"（精确机制）。

> 对齐：ai-tutoring 完善文档同结构 8 节；question-analysis 9 节 = 同结构 + 09 业务闭环补节。额外把"数据关联"拆为 3 节（05/06/07）——这是题型分析模块的核心差异区，值得厚写。

> **权威度 1.0 前置校验**：生成完善文档必须同时具备「语雀/design 方案素材」+「对应主题的代码分析文档作为落地真值」；**缺失代码证据，禁止输出 1.0，降级为 0.8**。不能仅凭设计稿生成 1.0 正文。

### 与 1.语雀 的区别（防混淆，2026-08-27 确认）

| 维度 | 1.语雀（产品意图/设计决策层） | 4.完善文档（权威正文层） |
|---|---|---|
| 组织维度 | 按**信息类型**（总揽/决策/选型/时间线/术语/边界/问题单） | 按**视角**（项目介绍/操作/难点/数据关联/架构/演进） |
| 权威度 | 0.8（意图层） | **1.0（正文层）** |
| 进池形态 | 切块进**切片池**（细粒度，doc_type=canonical） | 整篇进**全量池**（一节=一个完整答案） |
| 文档结构 | 各文档自带模板（决策 D# 块 / 选型对比表 / 时间线阶段） | 统一五段：为什么→方案→落地真相→追问防御→证据 |
| 回答的问题 | "为什么选题型不选知识点""累计平均 vs max 怎么权衡""CANDIDATE/STABLE 阈值哪来的" | "题型分析模块做什么""主流程怎么走""掌握度怎么算、存哪" |
| 说服力/直接可用 | 背景动机强，但**不能直接当答案**（是决策链/对比） | **可直接当答案**，一个视角整篇就是完整答复 |
| 对 RAG 的职责 | 切片池**设计意图补充**（决策的"为什么"与演进链） | 全量池**主答案**（权威 1.0，优先召回） |

> **检索规则**：先回权威正文（完善文档 1.0），正文答不了的"为什么/权衡/演进"才落到语雀意图层（0.8 切片池）——与 `doc_type` 来源区分同一逻辑。两者事实同源，但职责互补：语雀保留设计决策的"为什么"（装不进 8 节答案），完善文档把现状结论提炼成可直接作答的权威正文。

## 二、9 视角 × 当前节映射

| 视角 | 对应节 | ai-tutoring 对照 | 覆盖度 |
|---|---|---|---|
| 项目介绍 | `01 模块定位与核心价值` | 01 | ✅ 成节 |
| 操作流程 | `02 题型分析主流程怎么走` | 02 + 06(图片题专项) | ✅ 成节 |
| 数据关联 | `05 数据落库与掌握度` `06 题型动态聚集与向量` `07 题目知识点与图谱关联` | 05 | ✅✅✅ 3 节最厚 |
| 开发难点 | `04 防作弊与异常防护` | 04 + 07(流式性能) | ✅ 成节 |
| 架构设计 | `03 架构与微服务分工` | 03 | ✅ 成节 |
| 未来演进 | `08 演进路线` | 08 | ✅ 成节 |
| 业务流程 | `09 业务闭环与两域解耦`（2026-08-27 补齐） | ⚠️ 内嵌 | ✅ 成节 |
| 业务视角 | ⚠️ 内嵌(仅 01 追问段) | ⚠️ 内嵌 | ⚠️ 未独立（并入 01） |
| 数据存储 | ⚠️ 内嵌(05 表结构) | ⚠️ 内嵌 | ⚠️ 未独立（并入 05） |

## 三、每节梳理了哪些方案

| 视角 | 该视角回答的问题（梳理目标） | 落地出处 |
|---|---|---|
| 项目介绍 | 模块是什么 / 解决什么问题 / 核心主张（掌握度主体 = 题型不是知识点） | 01 |
| 操作流程 | 主流程一张图：贴题/答疑/图片三入口分流 → 学科门 → 题型识别 → 归并 → 落表 | 02 |
| 数据关联 | ★差异区：掌握度(05) + 动态聚集(06) + 图谱关联(07) 三张图 | 05/06/07 |
| 开发难点 | 防作弊 / 低置信不硬猜 / 慢修复 / 结构化兜底 / 换题短路 | 04 |
| 业务流程 | 业务闭环：答疑产生信号 → 落库 → 掌握度页展示 → 回流 decide；两域解耦（A 题目→题型 / B 题型→知识点） | 散在 01/02/05/07 |
| 架构设计 | Python 桥 + vector_type 路由 / 三端边界 / 为什么向量不 Java 直调 | 03 |
| 业务视角 | 面试向：对客户/学生/家长分别什么价值、与知识图谱的区别 | 仅 01 追问段 |
| 数据存储 | 数据存哪/什么形态：Python 不落库、Java 两表 + Redis + COS 向量桶 | 05/07 提及 |
| 未来演进 | Non-Goal + 阶段 0→4 路径 + 数据飞轮 | 08 |

## 四、覆盖缺口与决策（2026-08-27）

3 个内嵌视角判断是否独立成节，**标准 = 检索命中率**：

| 视角 | 判断 | 决策 |
|---|---|---|
| **业务流程** | 面试/答疑最常问"闭环怎么转"，现分散 4 节，全量池召回时被 05/07 瓜分 | ✅ **已独立成节**（`09 业务闭环与两域解耦`，2026-08-27） |
| **业务视角** | 01 追问段已有雏形（与图谱区别），独立价值弱 | **并入 01 不动** |
| **数据存储** | 表结构在 05，Python 不落库在 05/07 | **并入 05 补一张「存储拓扑表」即可**，不必须独立成节 |

> 补充兜底：切片池已有 `分析-07 数据流与存储` + `分析-08 图谱与知识点关联`（doc_type=code_analysis）专答"数据存哪/怎么流"类精确问题——内嵌视角查询侧已能兜住。

## 五、写作要点（与 3.代码 层的约定一致）

- 每节固定结构：`## 为什么（语雀）` → `## 怎么设计（方案）` → `## 落地真相（代码）` → `## 追问与防御` → `## 证据引用`
- 代码真相写 `文件:行号` 证据；方案与代码口径不一致处用 `> ⚠️ **翻转**` 标注——5/06/07 是差异最大节（掌握度主体、PENDING 语义、聚集依据均已翻转）；**节内多处小翻转在小节旁单独标注**，不只靠开头一句
- 元信息头：`> 模块=question-analysis ｜ 节=NN ｜ 问题表=<视角> ｜ 权威度=高（正文）` + `> COS路径: rag-source/question-analysis/完善文档/NN-*.md`
- 证据引用统一链到 `3.代码/分析-*.md`（代码真值）+ `1.语雀/` + `2.OpenSpec design 决策/`，**带小节/决策编号**（如 `design-xxx.md（D#6）`），不给无出处结论
- 流程图/文本链路**正文必有文字复述**（embedding 读不到 mermaid），关键链路信息不只在图里

## 六、RAG 运行时配置（文档之外，配套必做）

| 层 | 文档 | 权威度 | 切片 | doc_type | 元数据 | 召回策略 |
|---|---|---|---|---|---|---|
| 权威正文 | `4.完善文档/0N-*.md` | 1.0 | **整篇不切**（全量池 rag-full） | `business_full_doc` | authority=1.0 | 业务理解/面试/方案优先召回；整篇当答案上下文 |
| 代码证据 | `3.代码/分析-*.md` | 0.8 | 按 `##` 切（切片池） | `code_analysis` | authority=0.8 | 接口/字段/异常/降级细节必须引入核对；**完善文档协商细节以它为准** |
| 历史素材 | `2.OpenSpec design 决策/design-*.md`、`1.语雀/` | 0.7 | 按 `###` 切（切片池） | `design_spec` | authority=0.7, source=OpenSpec | 仅前两层无答案才召回，输出提示「来自历史设计文档，请核对代码确认实际落地状态」 |

**系统 prompt 强制约束（三层分层）**：
1. `business_full_doc`（1.0 完善文档）→ 业务理解、方案、面试问题，**优先使用，整篇不要拆分**；
2. `code_analysis`（0.8 代码证据）→ 问到接口/字段/异常/降级/具体实现细节，**必须引入此层核对落地行为**，校验完善文档描述是否与代码一致；⚠️ 1.0 完善文档是业务综合叙述，涉及精确代码细节务必以 code_analysis 为准；
3. `design_spec`（0.7 历史素材）→ 仅前两层无答案才召回，输出引用提示。

## 七、更新触发流水线（文档变更顺序，强制）

1. **先代码证据层，后权威正文**：代码/design 变更 → 先增量更新 `3.代码/分析-*.md` → 再增量更新 `4.完善文档/0N-*.md`（**禁止全量重写正文**，防止正文与底层证据不一致）。
2. **校验流程（更新后必跑）**：更新完善文档后使用辅助校验器复核——①所有 `> ⚠️ **翻转**` 是否仍成立；②证据引用的文件路径是否存在；③Non-Goal 有没有被误写为已实现。