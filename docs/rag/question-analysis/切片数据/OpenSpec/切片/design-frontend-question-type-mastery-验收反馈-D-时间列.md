# 验收反馈 D：时间列

> summary: 掌握度列表加时间列：展示 items[].updatedAt，可按时间排序（新→旧），后端无需加字段。
> 权威度: 0.7
> 模块: question-analysis
> COS路径: rag-slices/question-analysis/OpenSpec/design-frontend-question-type-mastery-验收反馈-D-时间列.md
> 类别：操作流程

---

### 验收反馈 D：掌握度加时间列

> 检索摘要：掌握度列表加时间列：展示 items[].updatedAt，可按时间排序（新→旧），后端无需加字段。

- **反馈**：掌握度列表加「时间」列（最近练习时间）。
- **现状**：`getMastery.items[]` **已返回 `updatedAt`**（该题型最近一次作答时间）——后端已实现，无需加字段。
- **前端**：列表加时间列，展示 `items[].updatedAt` + 可按时间排序（新→旧）；题型分析页 9.1 标注如需也可消费同字段。

> 证据：详见 `2.OpenSpec design 决策/design-frontend-question-type-mastery.md`（§验收反馈 D：掌握度加时间列）
