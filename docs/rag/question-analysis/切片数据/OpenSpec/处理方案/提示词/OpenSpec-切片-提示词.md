# 任务：OpenSpec design 素材 →【按 ### 小节切片】

## 文件元信息
> 用途: 读取 `2.OpenSpec design 决策/design-*.md`（历史设计素材层，doc_type=design_spec，权威 **0.7**），按 `###` 小节切成自包含 RAG 切片，输出到 `切片数据/OpenSpec/切片/`。
> 权威度: 0.7 ｜ 模块: question-analysis ｜ 类别：9 视角闭集（单切片一个类别）
> 切片方式：**提示词 + 大模型按 ### 切**（脚本导出方案已废弃，与 代码/坑档案/引导问题/语雀 对齐）。

## 角色
你是RAG语料切片助手。输入：OpenSpec design 技术设计文档（RAG 结构化重构版）全文。任务：把每份文档按 `###` 小节切成自包含切片——一个决策/背景/目标/风险/迁移/开放问题各一小节一块，输出独立 md 文件，直接可入切片池 rag-slice（source=OpenSpec）；**头部精简**（summary/权威度/模块/COS路径/类别/状态 6 行），机器元信息（entry_id/source_doc/status tag）由 QT⑥⑦ 摄入时从文件名+状态+层配置推导。

## 定位：素材溯源库，不作回答主来源（必须遵守）
- 本层权威度 **0.7**，是**历史设计文档素材**，回答「设计当初怎么想/为什么这么定/有哪些待决」，**不能当已上线功能**。
- 检索规则（QT⑦ 落地）：优先 0.8/1.0 真相源（语雀 canonical / 完善文档 / 代码），主库无答案才允许引用本层；**引用必须提示「该信息来源于历史设计文档，请核对代码确认实际落地情况」**。
- **状态标记是命门**：✅ 已落地 / ⚠️ 设计建议未落地 / ❓ 待决策——**禁止把 ⚠️/❓ 当已上线功能**，检索侧靠 status 标签 + 层规则识别。
- **⚠️ 切片阶段不做跨文档冲突比对，不自动生成 WARNING 标记**（本切片输入只有单份 design 文档，无外部文档上下文）；✅/⚠️/❓ 状态原样保留；WARNING 属于后续**人工对账环节**产出，不在本切片任务执行。

## 输入
源文档（`docs/rag/question-analysis/2.OpenSpec design 决策/`，6 份，**一份文档一批**，一次只喂 1 份；下表 `### 块数` 为人工统计**参考值，以文档实际解析为准，不硬卡死数字**）：

| 源文档 | ### 块数（参考） | 决策编号形式 | 设计文档主题 |
|---|---|---|---|
| `design-backend-kp-question-analysis-backend.md` | 18 | D1~D13（乱序） | 题目理解&关联（后端） |
| `design-backend-question-type-mastery-backend.md` | 15 | Decision 1~10 | 掌握度&题型聚集（后端） |
| `design-frontend-kp-question-analysis.md` | 14 | 决策 1~9 | 题型分析（前端） |
| `design-frontend-question-type-mastery.md` | 20 | 决策 1~10 + 验收反馈 A~D | 掌握度（前端） |
| `design-python-ai-tutoring-topic-mastery-signal.md` | 8 | 决策 1~5 | 掌握度信号（Python） |
| `design-python-question-type-mastery-python.md` | 10 | D1~D5 | 向量桥（Python） |

## 切片规则（按 ### 小节，一个 chunk = 一个文件）

| 源 `###` 小节 | 输出文件名 |
|---|---|
| `### 背景：...` | `design-{change}-Context.md` |
| `### 目标与非目标` | `design-{change}-Goals-Non-Goals.md` |
| `### D#/Decision #/决策 #：...` | `design-{change}-D{编号}-{短名}.md`（编号**统一归一化 `D{#}`**：Decision 1/决策 1 → D1） |
| `### 风险与权衡` | `design-{change}-风险与权衡.md` |
| `### 迁移计划` | `design-{change}-迁移计划.md` |
| `### 开放问题`（含"全部已决，后端已实现"类） | `design-{change}-开放问题.md` |
| `### 验收反馈 *：...` | `design-{change}-验收反馈-{A/B/C/...}.md` |
| 顶层 `# ==== 分节 ====`（如 python 文档 COS 向量桶） | **不是 chunk**——章节分隔线，其下 `###` 正常切，不单独成文件 |

`{change}` = 源文件名去 `design-` 前缀（如 `backend-kp-question-analysis-backend`）；`{短名}` = 小节标题去编号冒号后短名。**文件名全部小写，禁止大写/空格/特殊符号**。

## 硬性规则
1. **一个 ### 一文件，自包含**：单块能独立回答对应设计问题，不依赖其它块。
2. **原文保真 100%**：决策正文/属性表/表格/流程图文本/代码块**原样保留，不转述不改写**；**特别完整保留 风险与权衡/迁移计划/开放问题**（素材溯源价值所在）；只删 文档说明。
3. **状态保留**：源条目 `> 状态`（✅/⚠️/❓）原样进头部 `> 状态`。状态 tag（status_done/suggest/open）由 QT⑥⑦ 摄入时从 `> 状态` 映射，切片 md 头不写。**不自动生成 WARNING**（见定位⚠️）。
4. **summary 规则（完整保留，不裁剪）**：源条目 `> 检索摘要` **原样完整复用、不裁剪不叠加**——检索摘要即召回锚点，信息越全检索率越高；仅当缺失/只写标题时，补写 1-2 句富含核心实体与动作的检索摘要：`本条{决策/背景/目标}：{核心行为}；权衡：{关键取舍}`，不自由发挥长段落。
5. **头部精简（机器元信息摄入时推导，md 头不写）**：切片 md 头只保留 summary/权威度/模块/COS路径/类别/状态 6 行；`entry_id`（←文件名段：{change}-D{#}/Context/Goals-Non-Goals/风险与权衡/迁移计划/开放问题/验收反馈-{X}，超长子块追加 `-2/-3`）、`source_doc`（←文件名前缀 design-{change}.md）、`status tag`（← `> 状态` 映射）由 QT⑥⑦ 摄入时推导，不在切片头冗余。
6. **表格承载**：短表直接保留；宽横向大表优先拆竖向属性小表，无法拆保留在本小节内。
7. **块大小**：800~1500 token；超长在同小节内按 `####` 子块拆（文件名加 `-2/-3`）——子块必须自包含、可独立回答一部分问题，**禁止按行数对半切碎片**；子块带完整头部元信息（summary/entry_id/tags 适配子块内容，entry_id 追加 `-2/-3`）；证据指针同时指向父小节 + 本块子上下文。
8. **超小条目不合并**：一条决策/小节再短也独立成文件。
9. **证据指针**：每块末尾 `> 证据：详见 \`2.OpenSpec design 决策/design-{change}.md\`（§{小节}）`；**仅当真实存在关联才追加 `｜ 语雀-决策记录.md D# ｜ 完善文档 0X-*.md`，无关联不要模板式占位抄写**（无 D# 对应就删掉那段）。

## 明确丢弃（不输出）
1. 原文件标题行 + 原文件头元信息（`> summary`/`> 权威度`/`> 模块`/`> COS路径` 等原始头）
2. `## 文档说明` 整节
3. 草稿标记 / 迁移备注文本（`> 2026-XX-XX 决策：迁出...` 类元提示，spec文件整理已移除，若有残留删除）

## 输出格式（每个 ### 一个 md，输出 `docs/rag/question-analysis/切片数据/OpenSpec/切片/`）

> 源文档内 `> 状态` / `> 检索摘要` 提取至文件头部元字段；**正文不再重复复制这两行**，正文只保留业务内容（表格/代码/文字）。

```markdown
# {小节短名}
> summary: {源检索摘要原样，1-2 句向量锚点，不叠加长句}
> 权威度: 0.7
> 模块: question-analysis
> COS路径: rag-slices/question-analysis/OpenSpec/{文件名}.md
> 类别：{9 视角闭集单值}
> 状态：{源条目状态 ✅/⚠️/❓}

---

### {源小节原标题}
> 状态：{源条目状态}
> 检索摘要：{源条目检索摘要}

{小节正文：决策/表格/流程图文本/代码块，原文保留}

> 证据：详见 `2.OpenSpec design 决策/design-{change}.md`（§{小节}）｜ 语雀-决策记录.md D# ｜ 完善文档 0X-*.md（仅真实存在才追加）
```

## 类别规则（9 视角闭集，单切片一个类别）
按小节内容归，**每个切片只分配 1 个最贴合的主类别，取该小节核心主旨，禁止多类别**：
> 决策/架构/端点/向量路由 → 架构设计；
> 掌握度/数据表/两表分离 → 数据存储；
> 题型聚集/别名/关联/信号 → 数据关联；
> 接口契约/联调/失败语义/越权 → 开发难点；
> 前端展示/交互/验收反馈/菜单 → 操作流程；
> 设计原则/建议项/范围降级 → 业务视角；
> 背景/目标非目标 → 项目介绍；
> 开放问题 → 未来演进；
> 迁移计划：**上线部署迁移优先归架构设计；只有偏向后续迭代规划才归未来演进**。

**不得跳出闭集。**

## 输出前自检（切片必过）
1. ✅ 每份文档按 ### 全切（块数以实际解析为准），无缺无重；`# ==== 分节 ====` 分隔线未误成 chunk。
2. ✅ 全文无 文档说明/草稿标记/迁移备注 残留。
3. ✅ 类别全在 9 视角闭集，**单切片仅一个类别**。
4. ✅ 头部精简 6 行（summary/权威度 0.7/模块/COS路径/类别/状态），**无 entry_id/source_doc/tags 残留**；子块文件名追加 `-2/-3`。
5. ✅ 状态标记原样保留（✅/⚠️/❓）。
6. ✅ 决策正文/风险/迁移/开放问题 100% 保留，未删减业务信息；正文**保留源条目 `> 状态`/`> 检索摘要`**（原文保真，检索摘要即召回锚点）。
7. ✅ 每条自包含；超长拆分子块同样自包含、元信息完整、文件名后缀正确。
8. ✅ summary 源检索摘要原样完整复用，无裁剪；缺失用固定模板补写。
9. ✅ 证据指针**无无意义占位引用**，关联文档/决策编号真实存在才写入。
10. ✅ 切片阶段未自动添加 WARNING 标记。

## 使用提示
1. **一次处理 1 份文档**（避免上下文过长漏小节）；6 份按 输入表顺序。
2. 产出前清空 `切片数据/OpenSpec/切片/` 旧切片（保留 readme.md）。
3. **QT⑥⑦ 机器元信息推导（下游落地约定）**：`slice_corpus.py` 从文件名+头部推导，md 头不写机器字段——`entry_id` ← 文件名段（`design-{change}-D{n}-*`→{change}-D{n}、`design-{change}-Context.md`→Context、超长子块 `-2/-3` 后缀）；`source_doc` ← 文件名前缀（`design-{change}-*`→design-{change}.md）；`status tag` ← `> 状态` 映射（✅→status_done、⚠️→status_suggest、❓→status_open）；`source/authority/module` ← 层配置（source=OpenSpec、authority=0.7）。检索侧 `meta.authority == 0.7 && meta.source == "OpenSpec"` 识别素材层。
4. **检索规则（QT⑦ 落地）**：doc_type=design_spec，素材溯源库**不作回答主来源**——优先 0.8/1.0 真相源（语雀 canonical/完善文档/代码），主库无答案才允许引用；引用必须提示「该信息来源于历史设计文档，请核对代码确认实际落地情况」，禁止把 ⚠️/❓ 当已上线功能；⚠️/❓ 块靠 status 标签识别并降级。WARNING 冲突标记由人工对账环节统一产出，不在切片层。
5. 新模块复用：改 模块 id / 源路径 / 输出目录 / 输入表（文档清单+### 块数+主题）。
