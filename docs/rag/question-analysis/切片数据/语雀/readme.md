# 切片数据 / 语雀

> 来源：`1.语雀/语雀-*.md`（产品意图/设计决策层，doc_type=canonical，权威 **0.8**）
> **本次范围 = 分片（按条目切片），不做 jsonl / 入桶**（QT⑥⑦ 等全部来源切完一起做）。
> 切片方式：**提示词 + 大模型按条目切**（脚本导出方案已废弃，2026-08-27 与 代码/坑档案/引导问题 对齐）。

## 一、源头（语料就绪）

- 源文档：`1.语雀/语雀-方案总揽.md` + `语雀-决策记录.md`（D1~27）+ `语雀-方案选型对比.md`（选型1~11）+ `语雀-演进时间线.md`（阶段0~4）+ `语雀-边界场景清单.md`（场景1~24）+ `语雀-前端联调问题单.md`（问题1~16）+ `语雀-术语对照表.md`
- 生成提示词：`1.语雀/处理方案/提示词/`（5 独立单任务 + 辅助 A/B/C，各自自包含通用底座）——产出上面这些 canonical 源文档
- 前提：canonical 文档已按条目结构化（每条自带 `> 状态` + `> 检索摘要` + 属性表），切片只需按条目拆 + 头部标准化

## 二、切片（提示词 + 大模型按条目切）

> 切片方式 2026-08-27 改为提示词 + 大模型：决策 D#/选型/演进阶段/边界场景/问题单各一条一个文件（一个 chunk = 一条决策/选型/场景/问题），块带 `> 状态` + `> 检索摘要` 作向量锚点。提示词：`处理方案/提示词/语雀-切片-提示词.md`。

- 粒度：方案总揽按 h2 主题（§1~12，§13 指针跳过）；决策 D1~27 / 选型 1~11 / 演进阶段 0~4 / 场景 1~24 / 问题 1~16 各一条一文件；术语表整表一块（词汇基准）
- 保真：条目正文/属性表/mermaid 原文保留，只删 文档说明/全量总索引表/附录；状态（✅/⚠️/❓）如实标注
- 头部：精简 6 行（summary=源检索摘要**完整保留** / 权威度 0.8 / 模块 / COS路径 / 类别 9 视角闭集 / 状态），无 `｜来源/锚点/节`
- 机器元信息：entry_id/source_doc/status tag **不在 md 头写**，QT⑥⑦ 摄入时从 文件名+状态+层配置 推导（entry_id←文件名段、source_doc←文件名前缀、status tag←状态行映射）
- 冲突校验：与 方案-代码对账/坑档案 已记录 ⚠️翻转 矛盾时，头部加 `> WARNING`（正文不篡改）；本次 3 处（选型2/总揽3/总揽12）

## 三、切片结果与质量检查

- 本次结果：**96 块**（总揽 12 + 决策 27 + 选型 11 + 演进 5 + 场景 24 + 问题 16 + 术语表 1），全落 `切片/`
- WARNING 3 处：选型2（字符归一→向量层 已移除）、总揽3（obs 自动关联→Non-Goal 域B独立化）、总揽12（灰度迁移依赖已关闭的 obs 自动关联）
- 质量检查：编号无缺（D1~27/选型1-11/场景1-24/问题1-16/阶段0-4/总揽§1-12/术语1）、类别全在 9 视角闭集、权威度全 0.8、状态标记保留（done 64/suggest 11/todo 7/open 1）、5 行头 + entry_id/source_doc/tags 齐全、附录/索引表无残留、mermaid/ascii 图完整、证据指针齐全

## 四、检索规则（入桶/查询时生效）

- canonical 回答"为什么"（决策 D#/选型/演进），**优先于 0.7 design 素材**；引用 OpenSpec design 素材须提示核对代码（`../模块语料搭建SOP.md`）
- 与完善文档 1.0 主答案配合：1.0 全量池优先召回，0.8 canonical 补决策依据；**是设计意图补充，不能直接当权威答案**（那是完善文档 1.0 职责）
- 元过滤：`tags.entry_id == "D8"`、`tags.entry_tags 包含 status_suggest` 可精准定位（QT⑥⑦ 落地解析）

## 五、入桶 / jsonl（QT⑥⑦，本次不做）

> 本次范围停在**分片**。jsonl 生成、入桶等**等全部来源（代码/坑档案/引导问题/语雀/OpenSpec）切完一起做**（QT⑥⑦ 落地），此处仅留规则备忘不执行。`slice_corpus.py` 解析 `> entry_id`/`> source_doc`/`> tags` 三行 → jsonl `tags["entry_id"]`/`tags["source_doc"]`/`tags["entry_tags"]`（list 子键，不覆盖 tags 字典）。

## 目录结构

```
语雀/
├── readme.md              # 本文（处理说明 + 视图总览）
├── 处理方案/
│   └── 提示词/
│       └── 语雀-切片-提示词.md   # 按条目切片提示词
└── 切片/                   # 全部语雀切片（96 块，readme.md + 语雀-{文档}-{条目}.md）
```
