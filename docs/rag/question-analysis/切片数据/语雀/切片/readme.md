# 切片数据 / 语雀 / 切片

> 语雀全部按条目切片（80 块）在此目录。一个条目（决策/选型/场景/阶段/主题）一个文件，带机器可解析元标签，直接可入切片池 rag-slice 检索。
> 来源：`1.语雀/` 6 份 canonical 文档，由 `../处理方案/提示词/语雀-切片-提示词.md` 生成。
> 状态：2026-08-27 全量产出完成（80 文件；前端联调问题单 16 块已随源摘除）。

## 组成

| 源文档 | 条目粒度 | 块数 | entry_id 规则 |
|---|---|---|---|
| 语雀-方案总揽 | 按 h2 主题（§1~12，§13 指针跳过） | 12 | `总揽-{节号}` |
| 语雀-决策记录 | 按 D# | 27 | `D{编号}` |
| 语雀-方案选型对比 | 按 选型# | 11 | `选型{编号}` |
| 语雀-演进时间线 | 按 阶段# | 5 | `阶段{编号}` |
| 语雀-边界场景清单 | 按 场景# | 24 | `场景{编号}` |
| 语雀-术语对照表 | 整表一块（词汇基准） | 1 | `glossary-all` |

## 头部元信息（精简 5 行）

- `> summary` = 源条目 `> 检索摘要` **原样完整保留**（召回锚点，信息越全检索率越高）
- `> 权威度: 0.8` ｜ `> 模块: question-analysis` ｜ `> COS路径: rag-slices/question-analysis/语雀/...`
- `> 类别`：9 视角闭集（架构设计/数据存储/数据关联/业务视角/未来演进/开发难点/操作流程/业务流程/项目介绍）
- `> WARNING`：仅冲突条目（3 处：选型2/总揽3/总揽12）

> `> 状态`（✅/⚠️/❓）已从切片移除：未落地/待决策语义靠 **检索摘要标注**（如 `> 检索摘要【未实现建议】：`）+ **类别**（未来演进）兜底。
> 机器元信息（entry_id/source_doc）**不在 md 头写**，QT⑥⑦ 摄入时从 文件名+层配置 推导：entry_id←文件名段（`语雀-决策记录-D8-*`→D8、`语雀-演进时间线-阶段4-*`→阶段4、`语雀-方案总揽-1-*`→总揽-1）、source_doc←文件名前缀；无 status tag。

## 检索规则

- doc_type=canonical，回答"为什么这么设计/权衡/演进"；与完善文档 1.0 主答案配合（1.0 优先，canonical 补决策依据）
- 元过滤：`tags.entry_id == "D8"` 精准定位
- WARNING 条目检索时提示"以代码分析文档为准，请交叉核对"
