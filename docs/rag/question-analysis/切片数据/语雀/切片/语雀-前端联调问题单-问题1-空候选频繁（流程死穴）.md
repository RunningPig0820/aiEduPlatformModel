# 空候选频繁（流程死穴）

> summary: 同一道「鸡兔同笼」5 次调用 4 次返回 candidates=[]，学生没得选流程断在"待确认"，根因是无数据锚时 LLM 自由猜测。
> 权威度: 0.8
> 模块: question-analysis
> COS路径: rag-slices/question-analysis/语雀/语雀-前端联调问题单-问题1-空候选频繁（流程死穴）.md
> 类别：开发难点

---

### 问题1：空候选频繁（流程死穴）
> 检索摘要：同一道「鸡兔同笼」5 次调用 4 次返回 candidates=[]，学生没得选流程断在"待确认"，根因是无数据锚时 LLM 自由猜测。

| 属性 | 内容 |
|---|---|
| 现象 | 5 次调用 4 次 candidates=[]，学生没得选，流程断在"待确认" |
| 触发流程 | 题型分析贴题 → analyze-question → 候选生成 |
| 根因 | 无数据锚时 LLM 自由猜测，题型识别→知识点两段传递误差叠加 |
| 修复方案 | 封闭域约束选择（D15）：题目+年级知识点池 → LLM 从池里选 → 恒返回 top-N 绝不空 |
| 状态 | ⚠️ 待办（方案已对齐） |
| 证据 | 语雀-方案设计1-问题6 |

> 证据：详见 `1.语雀/语雀-前端联调问题单.md`（§问题1）｜ 语雀-决策记录.md D15、D27 ｜ 完善文档 07-题目知识点与图谱关联.md
