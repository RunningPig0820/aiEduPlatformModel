# COS 写入延迟
> summary: COS 向量写入存在异步生效延迟约 10 秒，业务侧禁止 put 之后立刻查询向量，编排层面容忍延迟。
> 权威度: 0.8
> 模块: question-analysis
> COS路径: rag-slices/question-analysis/语雀/语雀-边界场景清单-场景12-COS写入延迟.md
> 类别：开发难点
> 状态：✅

---

### 场景12：COS 写入延迟
> 状态：✅
> 检索摘要：COS 向量写入存在异步生效延迟约 10 秒，业务侧禁止 put 之后立刻查询向量，编排层面容忍延迟。

| 属性 | 内容 |
|---|---|
| 业务场景 | COS 写入延迟 |
| 触发条件 | put_vectors 后立即 query |
| 当前处理 | 已处理：容忍 ~10s 异步生效 |
| 兜底降级策略 | 建锚不立查 + 留重试 |
| 残余风险 | 联调预期要含延迟 |

> 证据：详见 `1.语雀/语雀-边界场景清单.md`（§场景12）｜ 完善文档 06-题型动态聚集与向量.md ｜ 坑档案.md J-QT1
