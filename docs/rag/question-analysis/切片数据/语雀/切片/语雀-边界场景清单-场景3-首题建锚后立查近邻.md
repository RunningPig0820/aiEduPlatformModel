# 首题建锚后立查近邻
> summary: 向量 put 之后索引存在约 10s 异步生效延迟，建锚后禁止立刻查询近邻，避免联调误判向量写入失败。
> 权威度: 0.8
> 模块: question-analysis
> COS路径: rag-slices/question-analysis/语雀/语雀-边界场景清单-场景3-首题建锚后立查近邻.md
> 类别：操作流程
> 状态：✅
> entry_id: 场景3
> source_doc: 语雀-边界场景清单.md
> tags: ["场景3","冷启动","status_done"]

---

### 场景3：首题建锚后立查近邻
> 状态：✅
> 检索摘要：向量 put 之后索引存在约 10s 异步生效延迟，建锚后禁止立刻查询近邻，避免联调误判向量写入失败。

| 属性 | 内容 |
|---|---|
| 业务场景 | 首题建锚后立查近邻 |
| 触发条件 | put 题型名向量后立即 query |
| 当前处理 | 已处理：put 后索引异步生效 ~10s，建锚后不立查（本身无近邻），留重试 |
| 兜底降级策略 | vector_store.py 注释"put 后约 10s 生效"；聚集编排容忍延迟 |
| 残余风险 | 联调期容易误判"向量没写进去" |

> 证据：详见 `1.语雀/语雀-边界场景清单.md`（§场景3）｜ 完善文档 06-题型动态聚集与向量.md ｜ 坑档案.md J-QT1
