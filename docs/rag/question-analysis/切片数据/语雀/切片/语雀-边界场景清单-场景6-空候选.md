# 空候选
> summary: 识别无候选返回空列表，Java 侧降级 PENDING，不调用掌握度接口；该问题需要 D15 封闭域约束方案补强。
> 权威度: 0.8
> 模块: question-analysis
> COS路径: rag-slices/question-analysis/语雀/语雀-边界场景清单-场景6-空候选.md
> 类别：开发难点
> 状态：⚠️

---

### 场景6：空候选（candidates=[]）
> 状态：⚠️
> 检索摘要：识别无候选返回空列表，Java 侧降级 PENDING，不调用掌握度接口；该问题需要 D15 封闭域约束方案补强。

| 属性 | 内容 |
|---|---|
| 业务场景 | 空候选（candidates=[]） |
| 触发条件 | 题型识别低置信/冷启动猜不出，无候选给前端 |
| 当前处理 | 空 topic_labels = Java 降级 PENDING |
| 兜底降级策略 | 前端展示候选/空态，到此为止，不查掌握度 |
| 残余风险 | 5 次调用 4 次空候选是流程死穴，需封闭域约束选择（D15 建议项）补强 |

> 证据：详见 `1.语雀/语雀-边界场景清单.md`（§场景6）｜ 语雀-决策记录.md D15 ｜ 完善文档 02-题型分析主流程怎么走.md
