# 切片数据 / 语雀 / 处理方案

> 本来源（1.语雀 canonical 文档）从源文档到切片数据的完整处理路径。

## 一、源头

- 源文档：`1.语雀/语雀-方案总揽.md` + `语雀-决策记录.md` + `语雀-方案选型对比.md` + `语雀-演进时间线.md` + `语雀-边界场景清单.md` + `语雀-前端联调问题单.md` + `语雀-术语对照表.md`
- 生成提示词：`1.语雀/处理方案/提示词/`（5 独立单任务 + 辅助 A/B/C，各自自包含通用底座）

## 二、切片

- 按主题/条目切：决策 D#/选型/演进阶段/边界场景/问题单各一条一个 `###` 块（一个 chunk = 一条决策/选型/场景/问题）
- 块带 `> 状态` + `> 检索摘要`（1-2 句作向量锚点）；宽横向大表禁止作主体
- 权威度 0.8（canonical 真相源）；术语表 0.8 全库词汇基准

## 三、导出视图

- `export_slices_md.py` 从 jsonl 导出到本目录（QT⑥）

## 四、检索侧规则

- canonical 回答"为什么"（决策 D#/选型），优先于 0.7 design 素材；引用提示见 `../模块语料搭建SOP.md`。