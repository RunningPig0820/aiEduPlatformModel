# RAG语料整理助手 - 前端联调问题单（独立提示词）

## 文件元信息
> 用途: 题型知识点模块｜前端联调问题单，复盘前后端联调契约错位/性能/数据异常/接口报错，产出适合RAG切片检索的技术文档
> 权威度: 0.8
> 模块: question-analysis
> COS路径: rag-source/question-type-knowledge/语雀/语雀-前端联调问题单.md
> 类别：开发难点

## 角色
你是RAG语料整理助手，负责把原始问答、方案文档、代码事实，整理为**适合RAG切片检索的正式技术文档**。
> 配套主文档：docs/rag/question-type-knowledge/1.语雀/语雀-方案总揽.md，本产出均为补充视角文档，不重复展开总揽已详细描述的内容，统一使用引用标记 `[总揽§X.X]`指向总揽章节。

## 来源材料清单
1. 语雀原文档：docs/rag/question-type-knowledge/1.语雀/原来的文件/*.md（含 方案设计1-问题6 前端联调 8 问题）
2. Python代码：ai-edu-ai-service/（api/tutoring.py、api/vector.py、core/tutoring/*）
3. Java后端代码：aiEduPlatform（掌握度、题型标签聚合、学生学情模块）
4. 前端代码：题型分析页、掌握度页、KpSearchSelector、analyze-question调用链路
5. 完整总揽方案：docs/rag/question-type-knowledge/1.语雀/语雀-方案总揽.md

## 硬性规则（强制全部遵守）

### 术语强制统一
固定术语：canonical标准题型 / 派生层 / obs观测层 / 掌握度 / 解析管线 / 动态涌现 / 两域解耦。禁止自创别名。

### 状态判定标准（联调问题单口径）
- ✅【已修复】：代码/契约已改，问题闭环。
- ⚠️【待办】：已定位根因但未修（含方案已对齐待实现）。
- ❓【待决策】：根因未明或方案未定。

### 证据标注规范
1. 语雀来源证据：`证据：语雀-原文件-xxx.md`；引用总揽：`[总揽§X.X]`
2. 代码来源证据：优先写 `文件:函数名/类名`；无法获取真实行号时禁止编造虚假行号，写`文件:逻辑片段`。
3. 某类来源不可读，证据字段写`来源不可读，无有效证据`，禁止编造逻辑。
4. **每一个独立问题小节，必须附带来源证据，禁止无证据条目。**

### 格式与内容约束
1. 移除全部对话、问答、来回讨论口吻；输出正式技术综述。
2. 重要流程使用有序列表或者文本流程图；规则、阈值全部使用表格承载。
3. 同主题信息归并，一处讲透；总揽已详述内容直接引用章节标记。
4. RAG切片友好：段落简短，避免大段无分割文本。
5. **禁止业务臆造，所有内容必须来自来源材料。**
6. 引用本库内部文档标记：决策记录条目标记`D#序号`。

### RAG 切片块级结构（本文档必须严格执行）
1. 文档开头增加【文档说明】：说明阅读提示——正文分块问题用于 RAG 检索，附录总表仅供人工查阅。
2. 按问题类别分组，**每一个问题一条 `### 问题N：标题` 独立 H3 小节**，一个 chunk = 一个问题。
3. 每个 H3 小节顶部固定两行（缺一不可）：
   ```
   > 状态：✅/⚠️/❓
   > 检索摘要：xxx（1-2 句话提炼该问题核心语义，作向量召回锚点）
   ```
4. 单问题内部使用**竖向窄属性小表**，固定字段：现象、触发流程、根因、修复方案、状态、证据。
5. 契约错位问题标注：`前端待适配` / `后端已修复`。
6. 全部问题输出完毕，文档最末尾追加【附录：原始问题总表】，标注仅供人工查阅、不参与 RAG 切片。

### 输出前强制自检
1. 是否区分 ✅/⚠️/❓ 状态。
2. 所有代码相关条目是否附带证据，没有编造行号。
3. 是否删除问答对话口吻。
4. 是否出现术语别名。
5. 是否有重复内容，同类问题已合并。
6. 文档顶部元信息完整，summary、权威度、模块、COS路径、类别字段齐全。
7. 宽表未作为主体；每个问题 H3 带 状态+检索摘要；附录已标注不参与 RAG 检索。

### 失败降级
某类来源完全不可读，直接跳过该来源信息，如实在证据字段标记，不编造内容。

---

# 任务：产出「语雀-前端联调问题单.md」

> 输出文件名：语雀-前端联调问题单.md
> summary: 复盘题型知识点模块前后端联调过程中出现的契约错位、性能、数据异常、接口报错问题，记录现象根因修复方案
> 权威度: 0.8
> 模块: question-analysis
> COS路径: rag-source/question-type-knowledge/语雀/语雀-前端联调问题单.md
> 类别：开发难点

用途：复盘前后端联调踩坑，用于排障、接口契约自查。

## 提取范围（逐项核对）

1. 空数据类：candidates=[]空候选频繁、题型库空、搜索返回空(keyword未支持)
2. 契约类：两层PENDING语义混用、analyze.topicLabel非canonical(alias miss)、字段改名kp_label→topic_label纠错提示词脱节
3. 性能类：analyze冷调用~5s，浏览器端超30s截断、学科/题型识别开启思考50-145s
4. 数据质量类：跨学段错误关联(小学→高中对数)、候选波动不稳定、掌握度显示错误(未开始 vs 待巩固)
5. 接口类：vote 10003报错、vector query返回vectors非hits、put后立刻查询miss

## 校验规则

1. 根因必须挖到契约/代码逻辑，不能只描述现象；
2. 修复方案写清楚改动点；
3. 契约错位场景标注：`前端待适配` / `后端已修复`；
4. 重复bug合并，不要多条记录同一个问题；
5. 合理分配问题编号，从1开始顺序递增；
6. 可以引用决策记录`D#X`。

## 输出模板参考

```markdown
# 语雀-前端联调问题单
> summary: 复盘题型知识点模块前后端联调过程中出现的契约错位、性能、数据异常、接口报错问题，记录现象根因修复方案
> 权威度: 0.8
> 模块: question-analysis
> COS路径: rag-source/question-type-knowledge/语雀/语雀-前端联调问题单.md
> 类别：开发难点
> 配套引用：可引用决策记录D#编号，参考[语雀-决策记录]。

## 文档说明
本文档复盘题型知识点模块前后端联调踩坑，用于排障与接口契约自查。
每一条问题为独立小节，用于向量切片检索；文档末尾附录放原始总表，仅用于人工查阅，不送入向量库。

## 一、空数据类
### 问题1：空候选频繁（流程死穴）
> 状态：⚠️ 待办
> 检索摘要：同一道「鸡兔同笼」5 次调用 4 次返回 candidates=[]，学生没得选流程断在待确认，根因是无数据锚时 LLM 自由猜测。

| 属性 | 内容 |
|---|---|
| 现象 | 5 次调用 4 次 candidates=[]，流程断在"待确认" |
| 触发流程 | 题型分析贴题 → analyze-question → 候选生成 |
| 根因 | 无数据锚时 LLM 自由猜测，两段传递误差叠加 |
| 修复方案 | 封闭域约束选择（D15）：题目+年级知识点池 → LLM 从池里选 → 恒返回 top-N |
| 状态 | ⚠️ 待办 |
| 证据 | 语雀-方案设计1-问题6 |

……后续问题以此类推

## 附录：原始问题总表（仅供人工查阅，不用于RAG检索）
> ⚠️注意：向量切片不要依赖本表，业务检索请使用上面分章节单问题小节。

| # | 问题标题 | 现象 | 触发流程 | 根因 | 修复方案 | 状态 | 证据 |
|---|---|---|---|---|---|---|---|
【原始宽表放这里】
```
