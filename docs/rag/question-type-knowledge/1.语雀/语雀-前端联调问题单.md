# 语雀-前端联调问题单

> summary: 复盘题型知识点模块前后端联调过程中出现的契约错位、性能、数据异常、接口报错问题，记录现象根因修复方案
> 权威度: 0.8
> 模块: question-analysis
> COS路径: rag-source/question-type-knowledge/语雀/语雀-前端联调问题单.md
> 类别：开发难点

> 口径：状态 = 已修复 / 待办 / 建议。契约错位标注 `前端待适配` / `后端已修复`。根因挖到契约/代码逻辑，不浮于现象。

## 一、空数据类

| # | 问题标题 | 现象 | 触发流程 | 根因 | 修复方案 | 状态 | 证据 |
|---|---|---|---|---|---|---|---|
| 1 | 空候选频繁（流程死穴） | 同一道「鸡兔同笼」5 次调用 4 次返回 candidates=[]，学生没得选，流程断在"待确认" | 题型分析贴题 → analyze-question → 候选生成 | 无数据锚时 LLM 自由猜测，题型识别→知识点两段传递误差叠加，猜不中返回空 | 封闭域约束选择（D15 建议）：题目+年级知识点池 → LLM 从池里选 → 恒返回 top-N 绝不空 | 待办（方案已对齐） | 语雀-方案设计1-问题6 |
| 2 | 题型库为空 | question-types total=0，聚合任务未沉淀 | 聚合任务凌晨定时跑，联调时看不到效果 | 聚合未触发/未沉淀，数据驱动权威命中路径未生效 | 聚合手动触发接口，联调即时验证沉淀 | 待办（P1） | 语雀-方案设计1-问题6 |
| 3 | 搜索兜底未落地 | 前端 KpSearchSelector 已就绪、已正确发 {keyword} 请求，搜索返回空 | 空候选时学生手动搜索教材知识点确认 | 后端 `POST /api/kg/knowledge-points` 未支持 keyword 参数 | 后端补 keyword 支持 | 待办（P0）`前端已就绪，后端待修` | 语雀-方案设计1-问题6 |

## 二、契约类

| # | 问题标题 | 现象 | 触发流程 | 根因 | 修复方案 | 状态 | 证据 |
|---|---|---|---|---|---|---|---|
| 4 | 两层 PENDING 语义混用 | 前端用一个 status 变量处理两类 PENDING，状态展示全错 | analyze 返回 PENDING（题型没认出）与 getMastery 返回 PENDING（知识点待确认）都被当"待确认" | 两个接口 PENDING 含义相反，共用一个判断变量必然混 | 分层判断（D14）：识别层 PENDING → 展示候选/空态到此为止；掌握度层 PENDING → 待确认 | 已修复（方案定稿）`前端需适配` | 语雀-方案设计2-问题1 坑2 |
| 5 | analyze.topicLabel 非 canonical（alias miss） | 学生鸡兔同笼练很多，掌握度页显示"未开始" | analyze 返回原始名"解一元二次方程" → 前端拿它查 getMastery（key="一元二次方程"）→ 等号 miss | analyze 返回前未过聚集 post-process，非 canonical；掌握表 key 是 canonical | 后端保证 analyze 返回 topicLabel = canonical（返回前过聚集） | 待办（P0）`后端需修` | 语雀-方案设计2-问题1 坑1 |
| 6 | 掌握度信号字段改名脱节 | 掌握度信号静默丢失（mastery_signals 为空） | kp_label→topic_label 改名 → 纠错提示词仍硬编码 kp_label → 模型输出旧名 → Pydantic 校验失败 → 反复纠错 → 掉兜底 | function calling schema（自动变 topic_label）与纠错提示词字段名脱节 | `_schema_instructions` 纠错提示词同步改 topic_label；测试断言纠错提示词含 topic_label | 已修复（Python 侧） | design-python-ai-tutoring-topic-mastery-signal Decision 2 |

## 三、性能类

| # | 问题标题 | 现象 | 触发流程 | 根因 | 修复方案 | 状态 | 证据 |
|---|---|---|---|---|---|---|---|
| 7 | analyze 冷调用慢 | curl 实测 ~5s，浏览器端偶发超 30s 被前端超时截断 | 题型分析首次调用 analyze-question | 冷调用 LLM 推理慢 + 无缓存 | 待优化（P2）；可用缓存/预热/流式 | 待办（P2） | 语雀-方案设计1-问题6 |
| 8 | 学科门/题型识别开思考拖慢 | 学科门判定一次 50~145s，答疑链路等死 | subject-classify/question-understand 用默认参数调 LLM | 开思考（thinking）模式下模型长思考，简单判定也拖到分钟级 | 统一慢修复：关思考（thinking disabled）+ 20s 超时 + 关 SDK 重试，快速失败走降级 | 已修复（Python 侧） | core/tutoring/subject_classify.py:66-73 |

## 四、数据质量类

| # | 问题标题 | 现象 | 触发流程 | 根因 | 修复方案 | 状态 | 证据 |
|---|---|---|---|---|---|---|---|
| 9 | 跨学段错误关联 | 小学「鸡兔同笼」被关联到高中「对数方程求解」 | 题型分析 → LLM 关联知识点 | 候选未按学生年级锚定过滤，跨学段混选 | 年级锚定 resolveGrade(studentId)，候选只在对应学段池内 | 待办（P0，方案已对齐） | 语雀-方案设计1-问题6 |
| 10 | 候选波动不稳定 | 同一文本多次调用结果不同（RESOLVED→对数方程/ PENDING 空候选/候选每次波动） | analyze-question 多次调用 | 无数据锚时 LLM 自由猜测，两段传递误差叠加 | 封闭域池内选择 + 候选稳定性保证（同文本尽量一致） | 待办（P1） | 语雀-方案设计1-问题6 |
| 11 | 掌握度显示错误 | 学生没做过该类题却显示"待巩固/待确认" | getMastery 查不到该题型 | 未命中题型（true miss）被当待巩固；alias miss（false miss）也被吞成未开始 | 未命中显示"未开始"灰态；alias miss 该修后端收敛，前端不吞 bug | 已修复（方案定稿）`前端需适配` | 语雀-方案设计2-问题1 坑3 |

## 五、接口类

| # | 问题标题 | 现象 | 触发流程 | 根因 | 修复方案 | 状态 | 证据 |
|---|---|---|---|---|---|---|---|
| 12 | vote 报错 10003 | analyze 候选 vote 报 10003 | 学生确认候选 → vote 接口 | candidates 未过镜像校验 | candidates 镜像校验（10003 已修） | 已修复（后端） | 语雀-方案设计1-问题6 |
| 13 | vector query 返回字段错位 | Java 桥解析 vector query 响应拿不到 hits | 调 /api/tutoring/vector/query | COS `query_vectors` 返回 `(resp, data)`，命中在 `data["vectors"]`，非契约假设的 `hits` | 契约按 COS 实际返回 `{"vectors":[{key,metadata,distance}]}` 对齐 | 已修复（Python 侧） | design-python-question-type-mastery-python D3 |
| 14 | put 后立查 miss | 题型名向量入库后立即 query 查不到近邻 | put_vectors → 立即 query_vectors | COS 向量索引 put 后 ~10s 异步生效 | 建锚不立查 + 留重试；put 后 query 容忍延迟 | 已修复（方案定稿） | design-python D3 |
