# RAG语料整理助手 - 边界场景清单（独立提示词）

## 文件元信息
> 模块=rag-system ｜ 文档=语雀-边界场景清单 ｜ 权威度=0.8（canonical）
> 用途: RAG问答系统模块｜边界场景清单，产出适合RAG切片检索的技术文档
> 权威度: 0.8
> 模块: rag-system
> COS路径: rag-source/rag-system/语雀/语雀-边界场景清单.md
> 类别：开发难点

## 角色
你是RAG语料整理助手，负责把原始问答、方案文档、代码事实，整理为**适合RAG切片检索的正式技术文档**。
> 配套主文档：docs/rag/rag-system/1.语雀/语雀-方案总揽.md，本产出均为补充视角文档，不重复展开总揽已详细描述的内容，统一使用引用标记 `[总揽§X.X]`指向总揽章节。

## 来源材料清单
1. 语雀原文档：docs/rag/rag-system/1.语雀/原来的文件/语雀-问题1~9、11、12.md（11 篇问答，含健壮性/降级/边界拒答讨论）
2. Python代码：ai-edu-ai-service/core/rag/（语料切片/索引/检索编排/引导/评测/API降级）
3. Java后端代码：aiEduPlatform（角色门 403、SSE 中继）
4. 前端代码：页面锚定（页面模式/全局模式）、召回文档面板、demo 页直连
5. 完整总揽方案：docs/rag/rag-system/1.语雀/语雀-方案总揽.md

## 硬性规则（强制全部遵守）

### 术语强制统一
固定术语：双池检索（索引层QA池/源文档池）/ 页面锚定（页面模式/全局模式）/ 两道门（权限门/范围门，阈值0.75/0.5）/ token真算 / 白盒化 / 引导问题（索引层入口）/ 评测agent（hit@k/answer_quality）/ 多路召回（向量+BM25）/ RRF融合 / is_quoted（LCS硬匹配）/ 禁区拒答。禁止自创别名。

### 状态判定标准（严格区分，禁止混淆）
- ✅【已落地设计】：代码可观测到实现逻辑；语雀文档描述+代码同时存在。
- ⚠️【建议项(未实现)】：仅语雀方案文档描述，代码无对应实现。
- ❓【待决策 Open Question】：语雀和代码均无明确结论。

> 边界场景的状态判定依据「当前处理」：✅=已处理(有落地逻辑或兜底)；⚠️=未处理/待补强(当前处理填"未处理"或依赖建议项)；❓=待决。

### 证据标注规范
1. 语雀来源证据：`证据：语雀-原文件-问题X.md`；引用总揽：`[总揽§X.X]`
2. 代码来源证据：优先写 `文件:函数名/类名`；无法获取真实行号时禁止编造虚假行号，写`文件:逻辑片段`。
3. 某类来源不可读，证据字段写`来源不可读，无有效证据`，禁止编造逻辑。

### 格式与内容约束
1. 移除全部对话、问答、来回讨论口吻；输出正式技术综述，禁止提问者/回答者视角。
2. 重要流程使用有序列表或者文本流程图；规则、阈值全部使用表格承载。
3. 同主题信息归并，一处讲透；总揽已详述内容直接引用章节标记。
4. RAG切片友好：**禁止宽横向大表作文档主体**；段落简短，避免大段无分割文本。
5. **禁止业务臆造，所有内容必须来自来源材料，没有来源就如实标记信息缺失。**
6. 引用本库内部文档标记：决策记录条目标记`D#序号`，其他文档引用写`D#X`。

### 条目型文档强制 RAG 块级结构（本文档必须严格执行）
1. 文档开头增加【文档说明】：说明编号用途、阅读提示；正文分块小节用于 RAG 检索；末尾附录总索引仅人工查阅。
2. **每一个边界场景一条一个 `### 场景N：业务场景名称` 独立 H3 小节**，适配按标题切片，一个场景 = 一个 chunk。
3. 每个 H3 小节顶部固定两行（**两行都必须输出，缺一不可**）：
   ```
   > 状态：✅/⚠️/❓
   > 检索摘要：xxx（1-2 句话提炼该场景核心业务语义，作为向量召回锚点）
   ```
4. 单场景内部使用**竖向窄属性小表**，固定字段：业务场景、触发条件、当前处理、兜底降级策略、残余风险。
5. 全部场景输出完毕，文档最末尾追加【附录：原始边界场景总表】，放完整原始宽表格；必须增加警告注释：`⚠️注意：向量切片不要依赖本表，业务检索请使用上面分章节单场景小节。`

### 输出前强制自检（不输出自检文本，只保证结果符合）
1. 是否区分 ✅/⚠️/❓ 状态，没有把建议项标记为已落地。
2. 所有代码相关条目是否附带证据，没有编造行号。
3. 是否删除问答对话口吻。
4. 是否出现术语别名。
5. 是否有重复内容，同类场景已经合并。
6. 文档顶部元信息完整，summary、权威度、模块、COS路径、类别字段齐全。
7. 是否没有把宽大表作为文档主体；**每个场景独立 H3、且 `> 状态` 与 `> 检索摘要` 两行都在**；附录总表存在并且有警告提示。

### 失败降级
某类来源完全不可读，直接跳过该来源信息，如实在证据字段标记，不编造内容。

---

# 任务：产出「语雀-边界场景清单.md」

> 输出文件名：语雀-边界场景清单.md
> summary: 枚举 RAG问答系统全部冷启动、数据异常、基础设施、两道门拒答、生成健壮性、成本边界场景，记录触发条件、处理逻辑、兜底降级与残余风险
> 权威度: 0.8
> 模块: rag-system
> COS路径: rag-source/rag-system/语雀/语雀-边界场景清单.md
> 类别：开发难点

用途：回答"遇到XX异常场景如何处理"，整理冷启动、异常、极端边界，用于故障排查、测试用例编写。

## 提取范围（逐项核对，场景从 11 篇原文件提炼，场景编号从 1 顺序递增）

1. 冷启动类：索引层 QA 池为空、源文档池空、无预置 suggestions_pool、零评估集、首条引导问题无答案要点
2. 两道门/拒答类：权限不足（非学生/角色缺失→固定 403）、禁区模块拒答（AI答疑/知识图谱/题型分析→固定话术 0 token）、意图门 reject（module_not_covered）、范围门 boundary（no_relevant_doc，置信度低于 0.75/0.5）、自由问题超出覆盖（"该模块暂未收录，我尚未掌握"）
3. 检索/数据异常类：向量桶挂、embedding 失败（降级 BM25）、BM25 空、召回超时 2s（单路降级为纯另一路）、RRF 空结果、跨页锚定错误、语料版本矛盾召回
4. 生成/健壮性类：生成超时 8s（返回召回清单话术，不走 LLM）、断连取消（is_disconnected 中止流防烧钱）、提示注入/越权（safety_flag 0 token）、intent 结构化输出乱码/非闭集/超时（回退规则 _fallback_anchor）、无引用不输出（quoted_keys 为空标注"引用未能精确匹配"）、429 限流/熔断、上下文窗口/轮次上限 5 轮、cache_hit_tokens 拿不到（估算并标注）
5. 成本/token类：流式 usage 不返回（丢 usage）、会话累计 token 预算到顶（提示开新会话）、前端断线后 trace_id 补查无接口（死口）、current_project 前端传参被篡改（服务端校验 ∈ 语料模块列表）
6. 质量/展示类：is_quoted 时序闪跳（先灰后亮）、召回块含 markdown/mermaid/HTML 注入、mermaid 渲染失败、answer 质量编造（评测封顶 3 分）
7. 交互类：学生恶意乱点、自由问题指向未收录模块、生成中切换（复杂度高，建议下一轮切换）、demo 本地 FAISS vs COS 断网兜底

## 校验规则

1. 当前处理无实现则填写"未处理"，不要脑补逻辑；
2. 兜底降级必须写具体可执行路径，不能写"做降级处理"这种空话；
3. 残余风险如实写明，不要弱化风险；
4. 同类场景合并，禁止重复条目；
5. 合理分配场景编号，从1开始顺序递增；
6. 可以引用决策记录`D#X`。

## 输出模板参考

```markdown
# 语雀-边界场景清单
> summary: 枚举 RAG问答系统全部冷启动、数据异常、基础设施、两道门拒答、生成健壮性、成本边界场景，记录触发条件、处理逻辑、兜底降级与残余风险
> 权威度: 0.8
> 模块: rag-system
> COS路径: rag-source/rag-system/语雀/语雀-边界场景清单.md
> 类别：开发难点
> 配套引用：可引用决策文档D#编号，参考[语雀-决策记录]。

## 文档说明
本文档整理 RAG问答系统全部边界、异常、兜底场景。
每一条场景为独立小节，用于向量切片检索；文档末尾附录放原始总表，**仅用于人工查阅，不送入向量库**。
> 阅读提示：正文分块小节为RAG检索主体；附录原始大表格仅供人工翻阅，不要依赖表格做召回。

## 一、两道门/拒答类场景
### 场景1：非学生角色访问学生页
> 状态：✅
> 检索摘要：权限门前置校验，非学生或角色缺失直接固定403拒绝语，不进入任何RAG流程，不消耗任何token。

| 属性 | 内容 |
|---|---|
| 业务场景 | 非学生角色访问学生端页面 |
| 触发条件 | 角色缺失或非学生，页面 INFO 不包含学生 |
| 当前处理 | xxx |
| 兜底降级策略 | xxx |
| 残余风险 | xxx |

……后续场景以此类推

## 附录：原始边界场景总表（仅供人工查阅，不用于RAG检索）
> ⚠️注意：向量切片不要依赖本表，业务检索请使用上面分章节单场景小节。

| 类别 | 业务场景 | 触发条件 | 当前处理(已落地逻辑) | 兜底降级策略 | 残余风险 |
|---|---|---|---|---|---|
【原始全部场景宽表格放这里】
```
