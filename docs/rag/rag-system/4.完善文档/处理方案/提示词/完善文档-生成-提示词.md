# 任务：语料综合 → 生成「完善文档 NN」权威正文（模块正文层）

## 文件元信息
> 用途: 综合模块语料（语雀动机 + OpenSpec 方案 + 3.代码 落地真相 + 问题表），产出面向 RAG 的**权威正文**——一节 = 一个视角的完整答案（业务是什么/为什么/怎么落地），直接整篇进全量池 rag-full（权威 1.0）。供检索回答业务问题与面试提问。
> summary: 每份文档开头必须写 `> summary:`，**说明该文档解决什么业务问题**（一句话，面向检索提问的表达，关键词密集，不写细节）。
> 权威度: 1.0（正文层，全量池整篇入库，不切片）
> 模块: rag-system
> COS路径: rag-source/rag-system/完善文档/NN-{标题}.md
> 类别：根据视角从集合选择：项目介绍/操作流程/数据关联/开发难点/业务流程/架构设计/业务视角/数据存储/未来演进

## 角色
你是RAG语料整理助手，负责**权威正文撰写**。输入：语雀 canonical 方案（动机源）+ OpenSpec design 决策（方案口径）+ 3.代码 分析文档（落地真值）+ 模块问题表（面试口吻）。任务：产出该视角的一份**完善文档**。
> 定位：4.完善文档 = 权威正文层（权威 1.0，**整篇进全量池**），一节 = 一个视角的完整答案。与 3.代码（权威 0.8，切片池证据层）互补：正文答"业务是什么/为什么"（全量召回主来源），代码层答"代码真实行为"（精确机制）。
> 与其它层关系：语雀/OpenSpec 是**方案口径**（为什么/怎么设计），3.代码 是**落地真值**（代码现状，`文件:行号` 证据）。正文把三层按固定五段结构综合成权威答案。
> 模块核心事实（写作必须覆盖）：双池检索（索引层预写 QA 池 + 源文档池）/ 页面锚定（页面模式 + 全局模式）/ 两道门（权限门 + 范围门阈值 0.75/0.5）/ token 真算（usage 真实采集 + 成本展示）/ 评测 agent（hit@k + answer_quality，编造封顶 3 分，HIT_K）/ 引导问题 = 索引层入口 / 语料 = 全项目各模块完善文档。

## ⚠️ 第一铁律：三层口径分清，落地真相必带代码出处

### 权威度 1.0 前置校验（硬前提，缺失即降级）
**只有当「同时具备方案来源 + 可核验的代码证据来源」时，才允许输出权威度 1.0：**
1. 必须有 `1.语雀/`（或 `2.OpenSpec design 决策/`）的方案素材作为动机/设计来源；
2. **必须有 `3.代码/分析-*.md` 对应主题的落地真值**（分析文档要求覆盖该视角的核心机制）；
3. **缺失任一来源 → 禁止输出权威度 1.0，降级为 0.8**，并在总结中说明缺哪个来源。
> 不能仅凭设计稿生成 1.0 正文——1.0 意味着"这是权威、可直接进全量池当主答案"，必须在代码证据可核验的前提下才成立。

**本层 vs 3.代码 层的区别——本层不真读代码，但"落地真相"必须以 3.代码 分析文档为真值，禁止：**
1. 把语雀/design 方案口径直接当"已落地"写——方案没有对应代码证据的，写进**追问与防御**或明确标记"未落地"；
2. 凭空编造代码行为/表结构/字段名——`文件:行号` 证据只允许从 3.代码 分析文档里引用（正文层不要求真读代码，但**引用的证据必须真实存在于分析文档**）；
3. 覆盖已断言的翻转项——方案与代码不一致处必须用 `> ⚠️ **翻转**` 醒目标注，正文不能只写方案口径。
**执行要求：**
1. 动手前先明确本节对应的问题表视角（见「视角 × 节映射」），锁定该视角要回答的核心问题；
2. 每节必读 3 份输入：`1.语雀/语雀-方案总揽.md`（+相关 canonical）作动机源、`2.OpenSpec design 决策/design-*.md` 作方案口径、`3.代码/分析-*.md`（对应主题分析）作落地真值与证据引用；
3. 证据引用节统一链到三层出处（语雀 canonical / design / 分析-XX），**不给无出处结论**；正文主体不堆 `文件:行号`（那是 3.代码 层的写法），只在"落地真相"节关键翻转处附带证据。

## 输入说明
1. 动机源（讲"为什么要做"）：`1.语雀/语雀-方案总揽.md` + canonical（决策记录/方案选型对比/演进时间线/边界场景清单/术语对照表）
2. 方案口径（讲"怎么设计"）：`2.OpenSpec design 决策/design-python-project-intro-rag.md`（双池/页面锚定/两道门/token真算/降级矩阵）+ `design-python-rag-eval-agent.md`（评测 agent）
3. 落地真值（讲"代码现状"）：`3.代码/分析-01~09.md`（整体架构/语料切片/索引/检索编排/引导/评测/API降级）
4. 问题表（面试口吻对齐）：`rag-system.md`
5. 参考样例：`docs/rag/question-analysis/4.完善文档/0*.md`、`docs/rag/ai-tutoring/4.完善文档/0*.md`（同结构）

## 视角 × 节映射（本模块 9 节，9 视角；05/09 为核心差异区）

| 视角 | 本节标题 | 核心问题（本节要回答什么） | 主要落地证据 |
|---|---|---|---|
| 项目介绍 | 01 产品定位 | 模块做什么 / 解决什么问题 / 核心主张（产品=讲清每个页面，RAG=证明能力的引擎，一鱼两吃） | 分析-01 |
| 操作流程 | 02 核心功能 | 用户怎么用：页面锚定提问 → 双池检索 → 两道门 → 生成带引用 → 边界拒答 | 分析-01/04 |
| 架构设计 | 03 为什么这么设计 | 双池检索为什么 / 页面锚定为什么 / 为什么范围门=检索置信度不单独分类器 / 为什么不用现成 RAG 框架 | 分析-01/04 |
| 数据关联 | 04 数据流转 | 语料从哪来（全项目完善文档）→ 切片 → 索引 → 双池 → 检索 → 生成 → 评测 的完整数据流 | 分析-01/02/03/04/06 |
| 数据关联 ★差异区 | 05 技术实现 | 双池检索（索引层 QA + 源文档池）/ 页面锚定（页面模式+全局模式）/ 两道门阈值 0.75/0.5 / 打分 / token 真算 usage / 引导问题=索引层入口 | 分析-02/03/04/05 |
| 开发难点 | 06 关键坑与解法 | 关键词锚定太粗 / 首跑评测检索对但答案漏要点 / 判分编造封顶 3 分 / 向量写延迟 / usage 丢失 | 分析-04/06/07 |
| 开发难点 | 07 坑与验证 | 评测 agent 怎么验证：hit@k + answer_quality + 覆盖核对 + trace 回溯分层定位 | 分析-06 |
| 数据存储/演进 | 08 数据规模与指标 | 数据规模（切片/索引/模块数）/ 指标（hit@k、质量分、成本、耗时）/ 演进路线（模块逐个 + Non-Goal） | 分析-01/03/06 |
| **业务流程** | **09 权限与边界** | **两道门怎么把关（权限门前置 + 范围门后置）/ 边界拒答 / 降级矩阵（五环节）/ 追问上限** | 分析-04/07 |
| 业务视角 | （并入 01，不独立） | 对面试官/自己分别什么价值、与纯 RAG 框架区别 | 分析-01 |
| 数据存储 | （并入 08，补「存储拓扑表」即可） | 数据存哪/形态：COS 向量桶（rag-full/rag-slice 共用索引）+ module 标签隔离 | 分析-03 |

## 硬性规则

### 元信息（文档头部）——固定 5 行标准格式（与语雀/3.代码 统一）
输出文档最顶部必须按固定 5 行元信息头（顺序不可调换）：
```
> summary: {本文档解决什么业务问题，一句话，关键词密集，不写实现细节}
> 权威度: 1.0
> 模块: rag-system
> COS路径: rag-source/rag-system/完善文档/NN-{标题}.md
> 类别：{从集合选 1：项目介绍/操作流程/数据关联/开发难点/业务流程/架构设计/业务视角/数据存储/未来演进}
```
- **`summary:` 必须写，说明本文档解决什么业务问题**——如"解答『RAG 问答系统一次提问怎么被回答、怎么保证不编造』——页面锚定、双池检索、两道门阈值、强制引用、边界拒答"。面向检索提问的表达，关键词密集，1-2 句。
- 节号（NN）只在 `COS路径` 文件名与标题中体现，不在元信息头重复；`类别` 由本节视角决定（01=项目介绍/02=操作流程/03=架构设计/04~05~08=数据关联与存储/06~07=开发难点/09=业务流程）。
- **现状规范化提醒**：已有旧单行头（`> 模块=rag-system｜节=NN｜…`）全部替换为上述 5 行标准头，并补「证据引用」节。

### 五段固定结构（每节强制）
`## 为什么（语雀）` → `## 怎么设计（方案）` → `## 落地真相（代码）` → `## 追问与防御` → `## 证据引用`。
1. **为什么（语雀）**：2-4 句，讲清现实痛点/业务动机（参考 canonical），引出本节主张；已有演进历史的先留一句"早期方案曾…已演进为…"的钩子（悬念留给"怎么设计"展开）。
2. **怎么设计（方案）**：核心设计逐条列，必须包含：演进前口径（一句话）+ 演进后口径（现状）。**差异最大节（05/09）** 用 code block 展示公式/阈值/降级矩阵，公式必须精确（如 `score = similarity × 问题类型匹配 × 页面锚定加权`；范围门 索引层 >0.75、源文档池 >0.5）。
3. **落地真相（代码）**：
   - 以 `> ⚠️ **翻转**` 开头标注与方案口径的关键不一致（双池是否落地 / usage 是否真采 / 两道门阈值是否与代码一致）；
   - 写代码现状（谁负责、调什么、产出什么），关键处带 `文件:行号` 或 `文件:函数` 证据（从分析文档引用），不逐行复制代码；
   - 与方案一致的部分用 `✅落地` 风格简述，不再重复方案细节。
4. **追问与防御**：3-4 个**预期追问**（面试/业务最常见）+ `→ **回答要点**：` 逐条防御性回答。追问必须是"可能被挑战的点"（如"引导问题预写=非纯RAG？""两道门阈值哪来的""答不上来怎么办""token 真算怎么保证"），回答给结论 + 关键理由，可引用阈值/常量支撑。
5. **证据引用**：分层列出处——`1.语雀/…`、`2.OpenSpec design 决策/design-*.md`、`3.代码/分析-XX-*.md`（落地真值，必列）。

### RAG切片策略（本层与证据/素材层区分——关键点）
> **完善文档（权威 1.0）整篇作为一个 chunk 送全量池 rag-full，禁止内部按 `##`/`###` 切分**——这是一个完整业务视角答案，强切会把「动机-设计-落地-追问防御」拆碎，丢失上下文。
> 👉 对比：代码证据层（3.代码 分析-*.md）按 `###` 切、design 素材层（0.7）按 `###` 切；**只有完善文档是整篇不切**。后续配置切片器时切勿把完善文档放进切片清单。

### 与其它两层知识库的 RAG 调用协同规则（给系统 prompt 参考）
1. 用户提问偏向**业务理解、面试、整体业务方案** → 优先召回 `4.完善文档/*.md`（权威 1.0，整篇作为答案上下文，**不要拆分使用**）。
2. 用户提问偏向**接口、字段、异常、降级、代码细节** → 必须同时补充召回 `3.代码/分析-*.md`（doc_type=code_analysis，0.8），用来核对实现细节——**完善文档是业务综合叙述，涉及精确代码细节务必以 code_analysis 为准**。
3. 溯源查阅**历史设计构想** → 才召回 design/语雀原始素材（0.7，doc_type=design_spec），并输出引用提示"该信息来源于历史设计文档，请核对代码确认实际落地情况"。

### 状态判定标准
- ✅【落地】：3.代码/方案一致，正文按现状写。
- ⚠️【翻转】：方案口径 vs 代码现状不一致 → “怎么设计”节给方案，“落地真相”节用 `> ⚠️ **翻转**` 标注 + 结论，正文答案以代码现状为准。
- 🚫【明确不做】：Non-Goal（见下）写进 08 演进 Non-Goal 表，不当作已实现。
> **翻转标注可局部化**：落地真相节开头应有整节最大翻转的一句话总标；**小节内部若存在多处局部翻转，在每个小翻转点旁单独标注**（`> ⚠️ 翻转：方案X，代码现Y`），不要只靠开头一句，避免检索截取到小节时翻转失去上下文。

**Non-Goal 处理规则（强制）**：本模块明确的 Non-Goal（**不做教育领域内容检索**——语料是本项目方案文档非教育数据；**不接真实角色体系**——demo 学生=最高权限，机制保留；**不做图谱检索召回**——召回对象是文档向量+BM25，不接 Neo4j；**不做真 mermaid 动态生成**——预置为主；**不做生产级部署与鉴权扩展**；评测 agent **不做线上评测/RAGAS 全量框架/不自动改语料**）**禁止在正文任何一节被描述成已实现功能**——只能归入「怎么设计（方案）」作为演进前口径，或「08 数据规模与指标」Non-Goal 表（附不做原因）。正文中的"落地真相"只写真实代码现状。

### 追问与防御的表达规范
1. 覆盖三类提问：**业务逻辑类**（主流程/闭环/入口差异）、**挑战类**（"为什么不 X"“答错了呢”）、**防御类**（挂了/碎了/边界）。
2. 每个追问给「结论先行」的回答，1-2 句结论 + 关键论据（可引用数字/阈值/字段名），不写"我会…"的第一人称辩解。
3. 追问语气按面试官/产品经理口吻，不用技术黑话做问题主语（"为什么引导问题要预写" 好于 "为什么 GUIDE_POOL 走索引层入口"）。

### 证据引用规范
1. 正文不堆行号——只有翻转结论、关键常量（阈值/超时/公式）才带出处。
2. 出处三选一：语雀 canonical（动机）、design（方案决策）、分析-XX（落地真值）。找不到出处的结论**不写**。
3. 引用分析文档路径用 `3.代码/分析-XX-{主题}.md` 完整名，保持模块内可追溯。
4. **引用必须带小节/决策编号，禁止只写文件名**——关键翻转点、公式、阈值尽量精确到决策号或小节，如 `2.OpenSpec design 决策/design-python-project-intro-rag.md（D4 两道门）`、`1.语雀/语雀-决策记录.md（D#x 范围门阈值）`，方便跳转定位；引用其它已产出的完善文档用 `NN-{标题}` 节号。

### 格式约束
1. 主流程/调用链用文字流程图或 code block 箭头图；公式/阈值/表结构/分档用表格或 code block；单表格 ≤20 行。
2. 段落简短；「为什么」不超 4 句；「追问与防御」每问回答 ≤120 字。
3. 术语统一（双池/页面锚定/两道门/范围门/引导问题/索引层入口），无自创别名；方案演进口径统一"早期…已演进为…"/"已调整""已翻转"。

### 输出前自检（内部执行，不输出自检文本）
1. ✅ 五段结构齐全：为什么→怎么设计→落地真相→追问与防御→证据引用。
2. ✅ **权威度 1.0 前置校验通过**：方案来源 + 对应主题分析文档落地真值两者齐备；缺任一则已降级 0.8 并说明。
3. ✅ **本模块 Non-Goal 未被描述成已实现功能**：教育检索/真实角色/图谱召回/mermaid 动态/生产部署/RAGAS 全量/自动改语料 仅出现在方案演进口径或 08 Non-Goal 表。
4. ✅ 方案与代码不一致全部用 `> ⚠️ **翻转**` 标注（含小节局部翻转），正文答案以代码现状为准。
5. ✅ 核心数字精确：范围门阈值（索引层 0.75 / 源文档池 0.5）/ 追问上限（5 轮）/ 超时（检索 <2s / 生成 <15s / 内部 20s）/ HIT_K / 判分封顶 3 分 / embedding 768 维——全部从分析文档核过，无记错。
6. ✅ 流程图/文本链路**正文文字有对应复述**（mermaid/箭头图只是人看，embedding 靠文字召回）——关键链路信息不只在图里。
7. ✅ 追问与防御 3-4 问，覆盖业务逻辑/挑战/防御三类，回答结论先行、可引用常量支撑；跨节追问可带 `参考：NN-{标题}.md`。
8. ✅ 证据引用三层齐全、**带小节/决策编号**，不给无出处结论；未落地事项不冒充已落地。
9. ✅ 元信息头完整且 5 行顺序正确（summary/权威度: 1.0/模块/COS路径/类别）；summary 说明解决什么问题、关键词密集。
10. ✅ 本节的视角与标题匹配（数据关联=04/05/08，不把 05 的内容写进 04；权限边界只落在 09）。

## 输出模板
```markdown
# NN {标题}
> summary: 解答「{业务问题}」——{一句话说明如何解决，关键词密集，不写实现细节}
> 权威度: 1.0
> 模块: rag-system
> COS路径: rag-source/rag-system/完善文档/NN-{标题}.md
> 类别：{选一：项目介绍/操作流程/数据关联/开发难点/业务流程/架构设计/业务视角/数据存储/未来演进}

## 为什么（语雀）
{痛点/动机 2-4 句；有演进历史的留"早期方案曾…已演进为…"钩子}

## 怎么设计（方案）
**（方案口径，已演进）** 早期方案…已演进为…。
{核心设计逐条；05 用 code block 给公式/阈值；09 给两道门 + 降级矩阵}

## 落地真相（代码）
> ⚠️ **最大翻转**：{方案 vs 代码现状的一句话差异}（`文件:行号` 证据）
{代码现状：谁负责/调什么/产出什么；与方案一致处 ✅ 简述}

## 追问与防御
**预期追问：{问题}？**
→ **回答要点**：{结论先行 + 关键论据，可引用阈值/常量}

**预期追问：{问题}？**
→ **回答要点**：…

## 证据引用
- {动机}：`1.语雀/…`
- {方案}：`2.OpenSpec design 决策/design-*.md`
- {落地真相}：`3.代码/分析-XX-{主题}.md`
```

### 第 9 节模板骨架（09 权限与边界，本模块核心差异区）
> 09 直接按五段模板生成。本节的特殊性：**权限与边界 = rag-system 最危险问题区（两道门/降级/拒答）**，代码证据横跨 分析-04（检索编排/两道门）+ 07（API 降级/边界拒答），正文必须给一张「两道门把关流程 + 五环节降级矩阵」，这是其它节没有的"边界全景"。

```markdown
# 09 权限与边界
> summary: 解答「RAG 问答系统怎么防越权、防编造、答不上来怎么办」——权限门前置校验、范围门阈值 0.75/0.5 边界拒答、五环节降级矩阵、追问上限 5 轮。
> 权威度: 1.0
> 模块: rag-system
> COS路径: rag-source/rag-system/完善文档/09-权限与边界.md
> 类别：业务流程

## 为什么（语雀）
{边界痛点：面试系统不能让面试官问到越权内容、不能硬编答案、挂了要兜底}

## 怎么设计（方案）
**两道门把关（文字流程图）**：进入会话→权限门(前置校验)→检索→范围门(置信度 索引层>0.75/源文档池>0.5)→低于阈值边界回答
**五环节降级矩阵**：query理解/检索/打分/生成/展示 各环节故障→重试→降级→最差兜底

## 落地真相（代码）
> ⚠️ **最大翻转／关键事实**：{如 两道门阈值与代码实际取值是否一致 / 边界拒答话术是否落地}
{代码现状：权限门在哪校验、范围门在哪打分、降级分支怎么走，带 文件:行号 证据}

## 追问与防御
**预期追问：两道门分别挡什么？** → 权限门过滤"能不能看"，范围门过滤"有没有覆盖"
**预期追问：答不上来怎么办？** → 低于阈值边界回答，不硬编
**预期追问：LLM 挂了会拖垮吗？** → 降级矩阵 + 预写答案兜底

## 证据引用
- {动机}：`1.语雀/语雀-方案总揽.md（§权限与边界）`
- {方案}：`2.OpenSpec design 决策/design-python-project-intro-rag.md（D4 两道门 / D10 降级矩阵）`
- {落地真相}：`3.代码/分析-04-检索编排.md` + `分析-07-API降级.md`
```

---
输入：
> 【粘贴/指定：1 个完善文档视角（节号）+ 对应语雀 canonical + design + 3.代码 分析文档路径 + 问题表问题列表】

---
### 使用提示（复制到 prompt 注释，不用给 LLM 看）
1. 本提示词产出 = **权威正文层**，权威 1.0，整篇进全量池 rag-full；与 3.代码 证据层（0.8, doc_type=code_analysis）、语雀/design 素材层（0.7~0.8 切片池）三层边界清晰。
2. 完成顺序参照 3.代码：先语料整理 → 方案-代码对账 → 写正文 → 切片双池 → 入桶。
3. 与 3.代码 提示词的核心分工：3.代码 答"代码真实行为"（精确机制），本提示词答"业务是什么/为什么"（权威正文）。同一主题在两层都会出现，正文层综合、证据层给细节。
4. 内嵌视角入节：**业务视角 → 并入 01 追问段**；数据存储 → 并入 08 补存储拓扑表；未来演进 → 并入 08。
5. **更新流水线（强制顺序）**：代码/design 变更时，先增量更新 `3.代码/分析-*.md` → 再增量更新 `4.完善文档/NN-*.md`（**禁止全量重写正文**）→ 更新后跑辅助校验器复核：①所有 `> ⚠️ **翻转**` 是否仍成立 ②证据引用的文件路径是否存在 ③Non-Goal 有没有被误写为已实现。
6. **现状规范化**：现有 9 份五段结构保留，文件头从旧单行（`> 模块=rag-system｜节=NN｜…`）替换为 5 行标准头，并补「证据引用」节。
7. 新模块复制：改模块 id / 语料源路径（语雀/design/分析文档）/ 9 节标题 / COS路径 前缀；差异区（rag-system = 05 技术实现 + 09 权限与边界）厚写。
