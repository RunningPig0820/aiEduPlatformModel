# 切片数据

> 语料切片的**人可读视图**。切片方式：提示词 + 大模型语义分组（见各来源 处理方案）。
> 状态：2026-08-28 建骨架（五来源 目录/readme/提示词 就位；代码分析-01~09、语雀 canonical 6 份、引导问题列表/合成问答 待生成，切片数待回填）。
> 口径：本模块切分边界/权威度/形态以 `../切片清单.md` 为准；双池方案对齐 ai-tutoring/question-analysis/knowledge-graph（全量池 rag-full 整篇 + 切片池 rag-slice 细块，共用索引靠 metadata.module=rag-system 隔离）。

| 来源 | 权威度 | 切片数 | 说明 |
|---|---|---|---|
| 代码（开发切片） | 0.8 | 待生成 | `代码/切片/` 语义分组（业务链路/代码事实/坑与对账），保留代码证据 |
| 代码（切片） | 0.8 | 待生成 | `代码/切片/` 面向面试，过滤代码噪声，对账转复盘 |
| 坑档案（开发切片） | 0.8 | 待生成 | `坑档案/切片/` 一条目一文件（V#/K#），原文照搬 + 双证据 |
| 坑档案（切片） | 0.8 | 待生成 | `坑档案/切片/` 面向面试，过滤证据噪声，复盘转述 |
| 引导问题（问答切片） | 1.0 | 待生成 | `引导问题/切片/` 一问题一文件（合成问答），逐问作答直接可用 |
| 语雀（canonical） | 0.8 | 待生成 | `语雀/切片/` 按条目切（总揽h2/决策D#/选型#/阶段#/场景#/术语整表），带 entry_id/source_doc/tags 元标签 |
| OpenSpec（design 素材） | 0.7 | 待生成 | `OpenSpec/切片/` 按 ### 切（2 份 design），正文检索摘要必带，素材溯源不作主来源 |

> 各来源目录 = `readme.md（来源切片说明）` + `处理方案/（处理路径 + 提示词 + 专项切片）`，参考 `1.语雀/处理方案/` 结构。
> 切分边界/口径以 `../切片清单.md` 为准。
