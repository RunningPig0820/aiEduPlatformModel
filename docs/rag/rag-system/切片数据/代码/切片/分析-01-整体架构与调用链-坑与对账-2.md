# 分析-01-整体架构与调用链-坑与对账-2

> summary: 整体架构与调用链坑与对账-2
> 来源: 切片 ｜ 锚点: 坑与对账-2
> 节: 分析-01-整体架构与调用链
> COS路径: rag-slices/rag-system/代码/分析-01-整体架构与调用链-坑与对账-2.md
> 类别：开发难点
> target: 开发对账

---

## 对账要点（续）

对账结论逐条复盘（原始方案 → 实际落地 → 结论）：

- **评测是否走降级**：原始方案 D3"评测走 rag-retrieval"；实际走真实检索原语不降级，向量挂了就暴露（`eval_agent.py:13-14, 235-242`）。✅落地——设计意图如此，代码明确。
- **降级矩阵**：原始方案 D10/语雀问题1 ③④"逐级降级矩阵（embedding挂→BM25/本地索引/大纲/原文）"；实际旧 query 端点三级降级（向量挂→纯 BM25、doubao 挂→references 当答案、无命中→拒答，`api/rag.py:83-142`）；白盒向量超时→空路、生成超时→写死话术 + 召回清单、boundary→固定话术；**无"返回页面大纲"一级**。✅大体落地——"返回大纲"一级未见，其余落地。
- **引导池来源**：原始 `guide_pool.py:4-7`"引导问题源=每模块自己的底座池"；实际 GUIDE_POOL 三模块（ai-tutoring/question-analysis/knowledge-graph），rag-system 无独立池、兜底 ai-tutoring（`guide_pool.py:309,313,319-321`）。⚠️部分——rag-system 池未建，语料整理中。
- **生成强制引用**：原始方案 D6/rag-system.md"强制引用、无引用不输出、跨页按页标注"；实际 `_GEN_SYSTEM` 硬性约束只基于召回语料、末尾统一贴参考来源、正文不插标记（`query.py:692-748`），引用来源格式"引用/锚点"两行（`query.py:743-748`）；**跨页按页标注未在 prompt 中落实**（无按页分段显式指令）。⚠️部分落地——强制引用落地；"跨页按页标注"prompt 中未见。
- **追问上限**：原始方案 D9/rag-system.md"追问限制 5 轮"；实际 Python 无状态不产 turns/close/轮次上限（`api/rag_assistant.py:9-12`），归 Java Redis，Python 侧代码未见 5 轮上限实现。🚫 Python 不做（Java 承接）。
- **打分公式**：原始方案 D5"score = similarity × 问题类型匹配 × 页面锚定加权"；实际 `rrf × authority × anchor_w`（`query.py:667`），"问题类型匹配"未单独成因子（类别过滤已废弃），相似度由 RRF rank 间接表达。⚠️演进——公式重构，RRF 取代 similarity。
- **语料来源**：原始方案 D1"语雀+代码+OpenSpec → 完善文档 8 节"；实际实测切片池 tags.source 含引导问题/语雀-*/设计/坑档案/完善文档 1-8，全量池 23 块含语雀 0.7/代码 0.8/完善文档 1.0。✅落地。

> 证据：详见 `3.代码/分析-01-整体架构与调用链.md`（§对账要点）
