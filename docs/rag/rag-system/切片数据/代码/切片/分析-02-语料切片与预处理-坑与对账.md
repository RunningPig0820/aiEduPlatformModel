# 分析-02-语料切片与预处理-坑与对账

> summary: 语料切片与预处理坑与对账
> 来源: 切片 ｜ 锚点: 坑与对账
> 节: 分析-02-语料切片与预处理
> COS路径: rag-slices/rag-system/代码/分析-02-语料切片与预处理-坑与对账.md
> 类别：开发难点
> target: 开发对账

---

## 隐性坑与注意事项

- **`slice_corpus.py` 不产 category/pool**：split/file 模式的 `_tags` 只有 8 字段，无 category；实际 ai-tutoring jsonl 的 category 是"从切片文件头 `> 类别：` 手动反写"补的（cos向量桶的字段.md:69 明确标注 ⚠️）。走 `slice_corpus --module` 直接产出的 jsonl 缺 category，需 md_to_jsonl 逆解析或补写
- **`recover_slices.py` 的 HEAD_RE 只认 3 行头**：`HEAD_RE` 期望 `summary/权威度|来源|锚点/模块|节` 后紧跟 `\n\n---\n\n`；实际 ai-tutoring 切片 md 头在 `模块|节` 后还有 `> COS路径:` 与 `> 类别：` 两行，HEAD_RE 的 search 会落空 → summary 恢复全失败（`recover_slices.py:22-26` vs 实际 md 头）
- **`export_slices_md.py` 与 md_to_jsonl 头字段不对称**：block_md 只写 3 meta 行（summary/权威度|来源|锚点/模块|节），不写 COS路径/类别；但 md_to_jsonl 逆解析期望 COS路径/类别（缺了只 warn）。两脚本对"头应含哪些字段"定义不一致
- **gen_summaries / gen_full_summaries 硬编码 ai-tutoring 路径**：只读写 `rag_slices.jsonl` / `rag_slices_full.jsonl`，不支持 --module；多模块（question-analysis 等）摘要靠 md 头 `> summary:` 直读，不走这两个脚本
- **`slice_full.py` 硬编码 ai-tutoring**：CORPUS 写死 `docs/rag/ai-tutoring`，summary 复用 `rag_slices.jsonl.bak-20260826`；跨模块全量池要另写脚本（如 question-analysis/02_full_jsonl.py）
- **`file_path` 双口径**：`slice_corpus._tags` 的 file_path = 相对语料根 `docs/rag/<module>/` 的路径（前端定位源文件）；`md_to_jsonl`/`01/02` 的 file_path = 头 `> COS路径:`（COS key，如 `rag-slices/...` / `rag-source/...`）。两条流水线产出同一字段、语义不同
- **COS metadata 硬限制**：metadata 单条 ≤10 entries、单条 ≤2048B，summary 按"非 summary 值字节 + 250B 安全量"截断（`build_index.py:143-165`）；text 全文不进 metadata，命中后按 key 反查 jsonl
- **切块 key 冲突**：同 (file, anchor) 内段落拆多块，靠 chunk_idx 区分；导出 md 时同名文件加 `-2/-3` 后缀防覆盖（`build_index.py:134-142`；`export_slices_md.py:111-114`）

## 对账要点（方案 vs 代码现状复盘）

**⚠️翻转：代码分析切分等级——方案按 `###` 语义分组双切片 vs 代码 split_level=2（h2）且无 target/双切片字段**
原始方案（切片清单.md）：代码分析"按 `###` 语义分组双切片（开发向+面试向）"；代码现状：`slice_corpus.py` LAYERS 代码层 split_level=2（h2），且无 target/双切片字段（slice_corpus.py:36,146-161）。结论 ⚠️ 需 per-module 脚本或改 LAYERS。

**⚠️翻转：语雀权威度——方案 rag-system 切片清单 canonical 0.8 vs 代码硬编码 0.7**
原始方案：rag-system 切片清单语雀 canonical 0.8；代码现状：`slice_corpus` LAYERS 语雀硬编码 0.7（slice_corpus.py:34）。结论 ⚠️ 需改 LAYERS 才对齐。

**⚠️翻转：头部 5 行标准——方案 summary/权威度/模块/COS路径/类别 vs 导出脚本只写 3 meta 行**
原始方案：每模块流水线-tasks.md:47 标准头 = summary/权威度/模块/COS路径/类别；代码现状：`export_slices_md.block_md` 只写 3 meta 行（无 COS路径/类别），实际 ai-tutoring md 头有 5 行（人工补齐）。结论 ⚠️ 脚本未产 COS路径/类别。

**⚠️翻转：recover_slices 恢复——注释"应=234 块" vs 实际 294 块 + 头格式敏感**
原始口径：`recover_slices.py:78` 期望"应=234 块"；代码现状：当前 `rag_slices.jsonl` 实为 294 块；HEAD_RE 对含 COS路径/类别的实际 md 头不匹配。结论 ⚠️ 数字过期 + 头格式敏感。

**⚠️翻转：file_path 语义——方案相对语料根路径 vs 实际 COS key**
原始口径：`slice_corpus._tags` 注释"相对语料根路径"（前端定位源文件）；代码现状：`md_to_jsonl`/`01/02` 的 file_path = COS key（rag-slices/... 或 rag-source/...）。结论 ⚠️ 双口径。

**⚠️翻转：doc_type=code_analysis——方案要求该字段 vs 代码 build_index 已去掉**
原始方案：代码深读提示词要求 doc_type=code_analysis；代码现状：`build_index` 已去掉 doc_type（与 module 同值冗余，2026-08-26），COS metadata 用 module 字段承载（build_index.py:144-148）。结论 ⚠️ 概念在，字段已折叠。

**✅落地：全量池块数——注释"23 块" vs 实际 23 块**
原始口径：`slice_full.py:15` 注释"23 块"；代码现状：实际 23 块（语雀 5 + 完善文档 8 + 代码 10）。结论 ✅ 对齐。

**✅落地：检索摘要——方案每个子块带 `> 检索摘要：` vs 代码不生成仅作正文保留**
原始方案：语雀 canonical/design 每个子块带 `> 检索摘要：`；代码现状：切片脚本不生成检索摘要，仅作为正文保留；代码证据层不强制写（代码深读提示词.md:84-85）。结论 ✅ 设计如此。

> 证据：详见 `3.代码/分析-02-语料切片与预处理.md`（§隐性坑与注意事项 / §对账要点）
