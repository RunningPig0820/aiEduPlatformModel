# 切片数据 / 语雀

> 来源：`1.语雀/语雀-*.md`（产品意图/设计决策层，doc_type=canonical，权威 **0.8**）
> **本次范围 = 分片（按条目切片），不做 jsonl / 入桶**（QT⑥⑦ 等全部来源切完一起做）。
> 切片方式：**提示词 + 大模型按条目切**（脚本导出方案已废弃，2026-08-28 与 代码/坑档案/引导问题 对齐）。

## 一、源头（语料就绪）

- 源文档：`1.语雀/语雀-方案总揽.md` + `语雀-决策记录.md`（D#）+ `语雀-方案选型对比.md`（选型#）+ `语雀-演进时间线.md`（阶段#）+ `语雀-边界场景清单.md`（场景#）+ `语雀-术语对照表.md`（**6 份 canonical，待生成**；条目数待生成）
- 原始素材归档：`1.语雀/原来的文件/*.md`（原始 11 篇语雀，证据源，不进池）
- 生成提示词：`1.语雀/处理方案/提示词/`（canonical 生成提示词）——产出上面这些 canonical 源文档
- 前提：canonical 文档待生成且需按条目结构化（每条自带 `> 检索摘要` + 属性表），切片只需按条目拆 + 头部标准化

## 二、切片（提示词 + 大模型按条目切）

> 切片方式 2026-08-28 定为提示词 + 大模型：决策 D#/选型/演进阶段/边界场景 各一条一个文件（一个 chunk = 一条决策/选型/场景），块带 `> 检索摘要` 作向量锚点。提示词：`处理方案/提示词/语雀-切片-提示词.md`。

- 粒度：方案总揽按 h2 主题（§N）；决策 D# / 选型 # / 演进阶段 # / 场景 # 各一条一文件；术语表整表一块（词汇基准）
- 保真：条目正文/属性表/mermaid 原文保留，只删 文档说明/全量总索引表/附录；已落地/未落地语义靠 **检索摘要标注（【未实现建议】/【待决策】）** + **类别** 兜底（`> 状态` 已从切片移除）
- 头部：精简 5 行（summary=源检索摘要**完整保留** / 权威度 0.8 / 模块 / COS路径 / 类别 9 视角闭集），无 状态/`｜来源/锚点/节`
- 机器元信息：entry_id/source_doc **不在 md 头写**，QT⑥⑦ 摄入时从 文件名+层配置 推导（entry_id←文件名段、source_doc←文件名前缀）；无 status tag（状态已删）
- 冲突校验：与 方案-代码对账/坑档案 已记录 ⚠️翻转 矛盾时，头部加 `> WARNING`（正文不篡改）；本次待对账产出后人工标注

## 三、切片结果与质量检查

- 本次结果：**待生成**（以 canonical 产出 + 实际条目数为准）
- 质量检查：编号无缺、类别全在 9 视角闭集、权威度全 0.8、头部精简 5 行无 状态/机器行残留、附录/索引表无残留、mermaid/ascii 图完整、检索摘要完整保留、证据指针齐全

## 四、检索规则（入桶/查询时生效）

- canonical 回答"为什么"（决策 D#/选型/演进），**优先于 0.7 design 素材**；引用 OpenSpec design 素材须提示核对代码（`../模块语料搭建SOP.md`）
- 与完善文档 1.0 主答案配合：1.0 全量池优先召回，0.8 canonical 补决策依据；**是设计意图补充，不能直接当权威答案**（那是完善文档 1.0 职责）
- 元过滤：`tags.entry_id == "D#"` 精准定位（QT⑥⑦ 落地解析）

## 五、入桶 / jsonl（QT⑥⑦，本次不做）

> 本次范围停在**分片**。jsonl 生成、入桶等**等全部来源（代码/坑档案/引导问题/语雀/OpenSpec）切完一起做**（QT⑥⑦ 落地），此处仅留规则备忘不执行。`slice_corpus.py` 从文件名+层配置推导 entry_id/source_doc → jsonl `tags`（md 头无机器行、无 status tag）。

## 目录结构

```
语雀/
├── readme.md              # 本文（处理说明 + 视图总览）
├── 处理方案/
│   └── 提示词/
│       └── 语雀-切片-提示词.md   # 按条目切片提示词
└── 切片/                   # 全部语雀切片（待生成，readme.md + 语雀-{文档}-{条目}.md）
```
