# 切片数据 / 语雀 / 切片

> 语雀 canonical 6 份**按业务主题合并**（2026-08-28 完成，27 块）在此目录。一块=一个业务问题的完整答案（对齐 `业务主题清单.md` 20 主题），不再按文档结构(D#/选型#/阶段#/场景#/h2)拆。同主题跨文档内容靠 source_doc/D#/选型#/场景# 锚点区分。
> 来源：`1.语雀/` 6 份 canonical，口径：`../../业务主题清单.md` + `../处理方案/提示词/语雀-切片-提示词.md`。
> 状态：✅ 27 块（01 产品定位/03 双池/04 页面锚定/05 意图/06 召回/07 生成/08 两道门/09 权限/10 token/11 引导/12 评测/13 健壮性/14 数据流转/15 三端/16 语料/18 演进/19 待决策/20 术语，超长拆 -2/-3/-4）。

## 组成

| 源文档 | 条目粒度 | 块数 | entry_id 规则 |
|---|---|---|---|
| 语雀-方案总揽 | 按 h2 主题（§N） | 待生成 | `总揽-{节号}` |
| 语雀-决策记录 | 按 D# | 待生成 | `D{编号}` |
| 语雀-方案选型对比 | 按 选型# | 待生成 | `选型{编号}` |
| 语雀-演进时间线 | 按 阶段# | 待生成 | `阶段{编号}` |
| 语雀-边界场景清单 | 按 场景# | 待生成 | `场景{编号}` |
| 语雀-术语对照表 | 整表一块（词汇基准） | 1 | `glossary-all` |

## 头部元信息（精简 5 行）

- `> summary` = 源条目 `> 检索摘要` **原样完整保留**（召回锚点，信息越全检索率越高）
- `> 权威度: 0.8` ｜ `> 模块: rag-system` ｜ `> COS路径: rag-slices/rag-system/语雀/...`
- `> 类别`：9 视角闭集（架构设计/数据存储/数据关联/业务视角/未来演进/开发难点/操作流程/业务流程/项目介绍）
- `> WARNING`：仅冲突条目（对账产出后人工标注）

> `> 状态`（✅/⚠️/❓）已从切片移除：未落地/待决策语义靠 **检索摘要标注**（如 `> 检索摘要【未实现建议】：`）+ **类别**（未来演进）兜底。
> 机器元信息（entry_id/source_doc）**不在 md 头写**，QT⑥⑦ 摄入时从 文件名+层配置 推导：entry_id←文件名段（`语雀-决策记录-D#-*`→D#、`语雀-演进时间线-阶段#-*`→阶段#、`语雀-方案总揽-{节号}-*`→总揽-{节号}）、source_doc←文件名前缀；无 status tag。

## 检索规则

- doc_type=canonical，回答"为什么这么设计/权衡/演进"；与完善文档 1.0 主答案配合（1.0 优先，canonical 补决策依据）
- 元过滤：`tags.entry_id == "D#"` 精准定位
- WARNING 条目检索时提示"以代码分析文档为准，请交叉核对"
