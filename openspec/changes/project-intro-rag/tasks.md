## 1. 文档准备（语料）

- [ ] 1.1 完善「知识图谱」完善版设计文档（8 节：定位/核心功能/为什么/数据流转含mermaid/技术实现/坑/规模/权限；以语雀+代码为事实源）
- [ ] 1.2 完善其余模块完善文档（AI答疑 / 组织中心 / 学生知识点分析 / RAG功能点）
- [ ] 1.3 实现切片器（按章节切分 + metadata：page/doc_type/section/permissions/source_doc/order）
- [ ] 1.4 切片器测试（章节完整性、metadata 齐全、source_doc 全文保留）
- [ ] 1.5 生成索引层 QA 条目（每页 5~8 条：问题+答案要点+引用）
- [ ] 1.6 QA 条目测试（数量 5~8、结构齐全、引用存在）

## 2. 嵌入与索引

- [ ] 2.1 实现 embedding 封装（dashscope text-embedding-v3，768 维 + 维度校验）
- [ ] 2.2 embedding 测试（维度正确、异常抛错）
- [ ] 2.3 改造 vector_store：embed() 抓 resp.usage.total_tokens；启用 `rag: "rag-index"` 路由
- [ ] 2.4 实现 COS rag-index 构建脚本（`--clear` 幂等重建、demo 预建）
- [ ] 2.5 索引构建测试（幂等重建、mock COS）
- [ ] 2.6 双池写入（qa/source 按 doc_type 区分写入）

## 3. 检索（双池 + 页面锚定 + 多路召回）

- [ ] 3.1 实现双池检索核心（按 doc_type 区分索引层池/源文档池）
- [ ] 3.2 实现页面锚定过滤（page 模式锁页 / 全局模式跨页）
- [ ] 3.3 实现 BM25 关键词召回（jieba 分词）
- [ ] 3.4 实现多路融合（向量 + BM25 合并结果）
- [ ] 3.5 检索测试（双池命中、页面锚定、变体文案语义匹配、关键词兜底）

## 4. 打分与范围门

- [ ] 4.1 实现打分（相似度 × 问题类型 × 页面锚定加权；top-K 1~3 / 3~5）
- [ ] 4.2 实现范围门（阈值索引层 0.75 / 源文档池 0.5 → 边界流程）
- [ ] 4.3 实现边界话术（预写 2~3 条，含覆盖模块列表）
- [ ] 4.4 打分/范围门测试（阈值命中、超范围边界回答）

## 5. 权限门

- [ ] 5.1 实现权限门前置校验（page 权限点 vs 角色，无权限不检索不生成）
- [ ] 5.2 demo 默认学生=最高权限 + 角色可切换演示
- [ ] 5.3 权限门测试（放行 / 无权限拒绝 / demo 默认放行）

## 6. 生成与引用 + token usage

- [ ] 6.1 改造 ark_stream：请求体加 `stream_options.include_usage` + 解析结尾 usage chunk
- [ ] 6.2 usage 解析测试（纯函数测试，含无 usage 降级估算）
- [ ] 6.3 编写生成 prompt（只基于检索上下文、每条引用标来源页、无引用不输出正文）
- [ ] 6.4 实现生成（doubao 流式 + 引用校验 + 跨页按页标注）
- [ ] 6.5 生成测试（引用强制、跨页标注、usage 缺失降级）

## 7. 成本展示

- [ ] 7.1 实现成本累计与换算（每轮 prompt/completion + 累计 + ¥ 换算，embedding 单列）
- [ ] 7.2 实现成本展示返回（随回答一起给前端）
- [ ] 7.3 成本展示测试（累计正确、单价换算正确）

## 8. 会话与追问限制

- [ ] 8.1 实现会话状态（页面锚定、追问计数）
- [ ] 8.2 实现追问上限 5 轮 + 超限提示文案
- [ ] 8.3 会话测试（计数正确、超限拦截）

## 9. 降级与重试

- [ ] 9.1 实现降级矩阵（各环节降级钩子：embedding→关键词；COS→关键词；生成→召回原文/预写答案；全挂→边界）
- [ ] 9.2 实现重试（COS 指数退避重试 ≤2 次；LLM/embedding 不重试）
- [ ] 9.3 实现超时控制（检索 <2s、生成 <15s、超时降级）
- [ ] 9.4 降级测试（检索失败→关键词；生成失败→预写答案；全链路失败→边界话术）

## 10. 预置流程图

- [ ] 10.1 完善文档第 4 节含预置 mermaid（每模块一张数据流图）
- [ ] 10.2 实现命中"数据流"类问题 → 渲染预置 mermaid；无预置 → 动态生成兜底
- [ ] 10.3 流程图测试（预置渲染、无预置动态生成、渲染失败降级文本）

## 11. API 端点

- [ ] 11.1 实现 `/api/rag/*` 端点（页面锚定 + 问答 + 成本 + 边界，对齐 api.md）
- [ ] 11.2 鉴权（`x-internal-token`）
- [ ] 11.3 API 测试（端到端问答、鉴权失败、无权限）

## 12. 评估与演示

- [ ] 12.1 编写评估集 20 条 Q&A（每页 5 条）与预期回答
- [ ] 12.2 实现命中率统计（hit@k 计算）
- [ ] 12.3 demo 预建索引 + 本地兜底路径验证（断网降级）
- [ ] 12.4 维护引导问题变体文案（UI 引导问题 ↔ 索引层规范问题配对）
